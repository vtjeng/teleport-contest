# Expected leaderboard score

This tracker keeps exact-commit evidence snapshots, newest first. It is not an
implementation journal. Routine chunk validation stays in tests, commit
history, and the active checklist. Recorded correctness and simplification
frontiers and metrics stay in `QUALITY.json`; other required reports and the
removed pre-policy checkpoint rows remain available in their related checklist
evidence and Git history through
`eb0ae7f17e0141c35884de2ac833104f25f28dde`.

`shown` is the estimated 44-session public score. A published leaderboard
result supplies that value when available; otherwise a development result is a
conservative lower bound and does not scale from 33 to 44 sessions. `hidden`
is the separate official held-out score. Carried published values remain
uncertain after later code changes. No row below implies a local holdout run.

Keep one row per full code SHA and combine coincident triggers. The optional
candidate row marks a selected, fully validated handoff for the open slice. It
is replaced only by a later validated handoff, promoted if the same SHA meets a
permanent trigger, or deleted when a permanent snapshot supersedes it. It does
not advance merely because the code head advances.

| Code commit | Date | Trigger | Expected screen score | Score evidence and uncertainty |
| --- | --- | --- | --- | --- |
| `0b9717d3908f04719a7e35f8c5c1f576f4f8d83b` | 2026-08-01 | goal closure, review window closure, authorized holdout evaluation | `520 shown + 194 hidden = 714 total` | The pet-inventory goal closes with three slices, one full correctness pass and one light delta review, and **the reserved holdout is unchanged: 139 of 3,640 screens, 30,048 of 182,022 random-number values, 1 of 11 sessions, 0 replay errors — identical, digit for digit, to the descent goal's measurement at `20dd659` and to the three before that.** Development across the goal rose 496 to 520 of 7,765 screens and 106,505 to 107,227 of 610,816 random-number values, all of it in slice 1; slices 2 and 3 moved neither metric, which `scripts/scan-stops.mjs` had predicted at `dfa8a61` by showing that neither session the goal was selected for still stops on a pet-inventory boundary. **This is the fifth consecutive zero, and the goal was designed to be the test of why.** It was selected against the population the holdout samples rather than by `supports`: across 600 fresh seeds walking 96 keys on D:1, a starting-pet boundary was the port's first stop in 51 to 53% of walks. That prediction was written into `ROADMAP.md` before the work, and the answer is that the fresh-population census is no better than the column it replaced. **The generalization-failure protocol is not triggered.** It runs only where a review confirms fixture-specific or hardcoded behavior, and this goal ported whole C functions — `steal.c relobj()` and `mdrop_obj()`, `worn.c extract_from_minvent()`, `do.c flooreffects()`, `invent.c stackobj()`, with `display.c map_object()` as a prerequisite — each validated against seeds recorded fresh for the purpose, at 28 strict segments in `scripts/run-pet-drop.mjs` and 14 in `scripts/run-pet-carry.mjs`. **Review evidence.** The goal-closing correctness pass over `9a7aef52..7caa325` reported 17 raw findings, 15 deduplicated, 12 confirmed, 3 rejected, none unverified: eleven applied at `12706a7`, one deferred to `ROADMAP.md` (`u_on_newpos()` dropping C's `earth_sense()` call, whose excusing comment this range falsified by adding `domove()` as a caller). Its own audit-fix commit then took a light delta review over `7caa325..12706a7` — 12 raw, 8 deduplicated, 7 confirmed, 1 rejected — because an authorized holdout evaluation is a review deadline that audit-fix debt may not cross; all seven applied at `8bd0743`. Two of those seven were the orchestrator's own errors: a comment citing an `.nvmrc` this repository does not contain, and a corrected isolation flag that made `npm run checkpoint -- --focus` run every named file in one shared process, where the whole suite reports 2,379 of 2,380. **Uncertainty.** 63 of 361 mutants survived their first wave over the goal-closing range, clustered in `js/mklev.js` and `js/do.js` on generation constants and on `flooreffects()` arms whose two answers reach the same named refusal. Validation at this head: full suite, six generated-data checks and `check:namespace-members` pass; `npm run quality -- --check` reports gate and advisory both clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and is not this local holdout figure, which covers 11 sessions rather than the official set. |
| `dfa8a619d0716f9a0d57a4988b17b6376a63aa5b` | 2026-08-01 | behavior slice closure | `520 shown + 194 hidden = 714 total` | The pet goal's second slice, the turns between the pickup and the drop, and **it changed no `js/` line at all**: slice 1 deleted the refusal that stopped these turns and `js/dogmove.js` already carried `dog_invent():416-424` and `dog_goal()`'s 502, 551 and 576 arms, so what the slice owed was evidence that the port already matches C across them. **The development score is unchanged in both metrics, 520 of 7,765 screens and 107,227 of 610,816 random-number values, measured by the orchestrator with `npm run checkpoint` at this head.** The evidence is `scripts/run-pet-carry.mjs`: 14 fresh C segments over 44,497 random-number calls, 229 screens and 229 cursors, all strict, each ending at a command prompt with the pet still carrying and the prompt before it the same. Between them they cover both gates missing with `rn2(10)` never drawn, the first gate passing so the second is never drawn, `dog_goal():551` withholding an apport goal, `:576` closing the distance, and one pet whose earlier drop left `apport` at 2 rather than 3; hero-to-pet distance spans 1 to 20 and the carried object spans weapon, scroll, coin, spellbook, comestible and potion. Six relational, logical and boolean mutants sit on the validated lines and each was watched failing before the line was restored; `npm run checkpoint` reports 0 of 0 over the uncommitted `js/` diff because no `js/` line moved. **What this slice measured matters more than what it changed.** `node scripts/scan-stops.mjs` at this head shows neither of the two sessions this goal was selected for stopping on a pet-inventory boundary any longer: `seed1150-caveman-explore-move` stops at step 34 of 51 on `fire`, exactly where the forecast put it, and `seed0030-ten-diverse-deaths` stops at step 13 of 1,953 on `simple monster action requires pet ranged targeting`, having gained 5 of the 253 screens forecast for it. **The goal's remaining development headroom is therefore zero**, and slice 3's worth is the fresh-population incidence and the holdout alone. One measurement error is recorded in the commit: a hand-rolled replay loop that records state after each `moveloop_core()` undercounts by one monster turn, which had ended 13 of the 14 draft segments on slice 1's boundary rather than this one. The full suite, six generated-data checks and `check:namespace-members` pass; `npm run quality` reports the review gate clear with three areas at the advisory checkpoint. CI reports `success`. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `9a7a1807600febcc0d0b150231c0d77440fcc4e5` | 2026-08-01 | behavior slice closure, score change | `520 shown + 194 hidden = 714 total` | The pet goal's first slice: a tame starting pet puts down what it carries. `steal.c relobj()` (873-899) and `mdrop_obj()` (812-846) through `worn.c extract_from_minvent()` (1376-1416) and `do.c flooreffects()` (161-357), with `dogmove.c dog_invent():418` as the live consumer, and the `display.c map_object()` prerequisite landed separately at `36562b9`. **Development rose 496 to 520 of 7,765 screens and 106,505 to 107,227 of 610,816 random-number values, measured by the orchestrator with `npm run checkpoint` at this head.** Two sessions gained and none regressed: `seed1150-caveman-explore-move` 8 to 27 screens and 2,424 to 2,988 calls, `seed0030-ten-diverse-deaths` 8 to 13 and 6,484 to 6,642. Both were named in advance as the sessions this goal could move. That +24 sits inside the 11-to-58-screen band the goal entry predicted before the work, which is the first of this goal's predictions to survive its measurement. Deleting the blanket refusal on a pet carrying anything but an inert saddle was the prerequisite rather than a later tidy-up. The evidence is `scripts/run-pet-drop.mjs`: 16 recorded segments over 57,735 random-number calls and 179 screens, all strict, spanning the dropped object's class across ten of them, both `distant_name()` branches, three roles, both starting pet types, `!verbose` and `accessiblemsg`. **A recorded finding was falsified here and corrected at `393342c`:** `dog.c:60` sets `edog->apport = ACURR(A_CHA)`, but `allmain.c newgame()` calls `makedog()` at 814 and `u_init_inventory_attrs()` at 816, so `init_attr()` has not run and `acurr()` floors the value at 3 — a fresh recording of seed 8902029 with a Tourist reading `Ch:18` logs `rn2(3)`. The live gate is `!rn2(udist + 1)`, so the matrix varies hero-to-pet distance rather than charisma. **Uncertainty.** Two mutation survivors remain, both literal arguments at `js/steal.js:249`, which the equipped-object refusal puts permanently out of reach of any caller. Several arms stop by name and belong to slice 3: `relobj()`'s second and later objects, `mdrop_obj()` onto a square that already holds an object, and every `flooreffects()` arm that would destroy, damage, move, merge or announce. The full suite, six generated-data checks and `check:namespace-members` pass; `npm run quality` reports the review gate clear. CI reports `success`. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `20dd659` | 2026-08-01 | goal closure, review window closure, authorized holdout evaluation | `496 shown + 194 hidden = 690 total` | The descent goal closes with seven slices, one gate-forced correctness pass and one goal-closing pass, and **the reserved holdout is unchanged: 139 of 3,640 screens, 30,048 of 182,022 random-number values, 1 of 11 sessions, 0 replay errors — identical to the eating goal's measurement at `5b8061d`, digit for digit.** Development across the goal rose 487 to 496 of 7,765 screens and 100,910 to 106,505 of 610,816 random-number values. **This is the largest goal this port has taken and it carried over nothing, which is the most important thing this row records.** It was selected on the strongest evidence any goal has had: `scripts/scan-debt.mjs` rated `down` at 3,515 screens of `supports` across 8 of 33 development sessions, 45% of the whole recorded population, and it is gated by no role or prefix — the property that distinguished the two goals whose holdout gain was non-zero. The port now completes a level transition end to end: across two 12,000-seed censuses, 2,347 descents pressed `>` and none fails closed before D:2 is drawn. None of that reached the holdout. **The honest reading, and what it does not license.** `supports` measured what depends on the behavior, not what porting it earns, and the goal's own development gain says the same thing more cheaply: 3,515 screens of `supports` yielded 9. Every development session that gained had already been counted; the 8 sessions the census named still stop, now on what follows a descent rather than on the descent. That is consistent with an 11-session holdout lacking the resolution to see it, which `ROADMAP.md` has argued before and which three zeros could not settle either way — but it is now four zeros against goals selected four different ways, and the pattern is no longer plausibly about goal size alone. What it does **not** license is treating `supports` as broken: it ranked this behavior first and the behavior really was the port's largest single dependency. The gap is between unblocking a boundary and finishing the work behind it. **Uncertainty.** No fixture-specific or hardcoded behavior is suspected, so `.agents/validation.md`'s generalization-failure protocol is not triggered: the goal's evidence is 5 strict `runFreshMatrix()` differentials over 67 segments and five characters, none of them derived from a recorded session. The `shown` figure remains a conservative development lower bound and does not scale from 33 sessions to 44. Validation at this head: 2,353 tests, six generated-data checks, `check:namespace-members` and the development score all pass; the review gate is clear with three areas at the advisory checkpoint. |
| `f554d25` | 2026-08-01 | behavior slice closure, score change | `496 shown + 194 hidden = 690 total` | The descent goal's last slice, and the first in this goal since slice 3 to move the score: `dog.c mon_arrive()`'s `With_you` arm (468-480), so a follower that is neither tame nor `u.ustuck` arrives with the hero instead of stopping the descent. **Development rose 488 to 496 of 7,765 screens and 106,404 to 106,505 of 610,816 random-number values, measured by the orchestrator with `npm run checkpoint`.** One session moved and nothing regressed: `seed0015-valk-level2-pit-dog-wait`, 20 to 28 screens and 8,383 to 8,484 calls. The worker attributed the gain by re-inserting one guard alone and watching it revert exactly. That session now stops on `simple monster action requires trap activation`, the pet falling into a D:2 pit. **Two refusals came out, and only one was in the brief.** The first is the audit-deferred `mon_arrive()` condition, and the source evidence sits one line further than the finding stated: `monmove.c set_apparxy()`'s first branch is `mtmp->mtame || mtmp == u.ustuck || u_at(mx, my)` (2211), and `mon_arrive()` writes `u.ux`/`u.uy` into `mux`/`muy` two statements earlier at `dog.c:450`, so the early return fires for *any* follower rather than the two the old comment named. Deleting it exposed the second: `assertSimpleActionState()`'s `STRAT_ARRIVE` stop, which refused a state the port already handles, since `monmove.c dochug():704-708` clears that bit through `m_arrival()` and `js/monmove.js dochug()` already carries the clear. **The descent census is what closes the goal.** Two 12,000-seed scans on the same seeds, before and after. A petless Valkyrie: 2,347 descents pressed `>`; 2 stopped before, **1** after, at seed 7902379 on `monster attack on the hero` — which fires *after* D:2 is drawn, so it is combat rather than descent. A Samurai with a dog: the `STRAT_ARRIVE` guard stopped **97** descents and now stops none. **No path from `>` to the drawn D:2 screen fails closed.** Evidence is `scripts/run-leave-level.mjs`, now 15 strict `runFreshMatrix()` segments across five characters at 81,528 calls, 309 screens and 309 cursors; six are new, and five of those fail at the previous head. They also close this tracker's standing item that nothing exercised `do.c u_collide_m()`. **Uncertainty.** The selector's `rn2(5)` peaceful arm is pinned by unit test alone: across more than 60,000 scanned seeds every non-tame follower was hostile, and the script header says so. Mutation over the `js/` diff reports 0 mutants of 0 sites, the diff being deletions and comments, so the worker hand-mutated the selector five ways and the new `scripts/level-arrival.test.mjs` caught all five. Two recorded defects cost seeds again: `map_object()` at 8100289 and the `newsym()` infrared gap at 8061229, which moved every pet segment to human characters. Full suite, six generated-data checks and `check:namespace-members` pass; gate clear with three areas at the advisory checkpoint. CI reports `success`. |
| `c0afa09` | 2026-08-01 | behavior slice closure | `488 shown + 194 hidden = 682 total` | The last of the four shop slices, and with it every `shtypes[]` row: `shknam.c veggy_item()`, `shkveg()`, `mkveggy_at()` and `mkshobj_at()`'s three-way `atype` dispatch, plus `set_mimic_sym()`'s `s_sym < 0` and `rt == FODDERSHOP` arms, which deleted the two refusals the mimic slice had left for it. `SUPPORTED_SHOPS` and `shopType()` are gone. **The development score is unchanged in both metrics, 488 of 7,765 screens and 106,404 of 610,816 random-number values, measured by the orchestrator with `npm run checkpoint`** — the fourth consecutive shop slice to move nothing, exactly as predicted, because no development session stops at `mkshop()`. Evidence is `scripts/run-shop-deli-wand-health.mjs`, strict `runFreshMatrix()` at 15 segments, two characters, 95,982 random-number calls, 336 screens and 336 cursors. **The number that matters here is not the score but the descent census.** Over 12,000 fresh seeds, 2,341 reached a down staircase and pressed `>`; **2 stopped**, both on `mon_arrive()`'s non-tame-follower refusal at `js/dog.js:750`, seeds 7902379 and 7905523. The other 2,339 descended — 1,318 ending off the staircase through ordinary walk drift, 491 on a shopless D:2, 530 on a stocked shop. Nothing in `mkshop()` or `stock_room()` refuses any more. Those two seeds also dissolve the reason the correctness pass deferred that refusal, which was that no recorded case supplied a non-tame follower, so it becomes the goal's next slice rather than standing debt. **Uncertainty.** Five mutants survive, each explained at its site and each turning on the same fact — no `shtypes[]` row as generated distinguishes the two clauses of the `FODDERSHOP && stock > MAXOCLASSES` guard, nor reaches `stock >= MAXOCLASSES` in the reroll, nor ends `shkveg()`'s scan on `index < NUM_OBJECTS` before the class test ends it. One near-miss is worth recording as method: `mkveggy_at()`'s `obj && obj.otyp === TIN` first appeared unkillable through slime molds because `svc.context.current_fruit` happened to equal 1, the exact `spe` that `set_tin_variety()` writes; it was killed instead by pinning `spe === 0` on the shop's plain foods. The `map_object()` recolour defect fired a **third** time, at seed 7612489, and that seed was dropped. One pre-existing flake was seen and is not this slice's: `scripts/record-session.test.mjs`'s unexpected-signal case failed once under checkpoint load and passed twice in isolation. Full suite, six generated-data checks and `check:namespace-members` pass; gate clear with two areas at the advisory checkpoint. CI reports `success`. |
| `a61ffab` | 2026-08-01 | behavior slice closure | `488 shown + 194 hidden = 682 total` | The descent goal's third shop slice: the second-hand bookstore and rare books, the two rows sharing the `shkbooks` name list, with `mkshobj_at()`'s 3.6 tribute arm at `shknam.c:461-468`. 13% of rolls, exact from `shtypes[]`. **The development score is unchanged in both metrics, 488 of 7,765 screens and 106,404 of 610,816 random-number values, measured by the orchestrator with `npm run checkpoint`,** so no session needed a first-divergence measurement and the ratchet was untouched. Almost nothing needed writing: `get_shop_item()` already walked both `iprobs[]` pairs, `mkobj_at()` already made scrolls and spellbooks, and `js/obj.js mksobj()` already carried the `SPE_NOVEL` finalization that draws its title through `noveltitle()`. Evidence is `scripts/run-shop-books.mjs`, strict `runFreshMatrix()` at 9 segments, 47,831 random-number calls, 169 screens and 169 cursors, which passed on its first run; `run-shop-descent.mjs` and `run-shop-mimic.mjs` still pass unchanged. **This slice is the cleanest of the shop set and the reason is worth recording:** it is the first to land on foundations three earlier slices had already built, and the first in this goal to hit no defect at all. The `map_object()` recolour never fired and no seed was dropped, which narrows that entry further — two slices have now stocked affected classes without reaching it. **Uncertainty.** One mutant survives of five, on the `artif` argument to `mksobj_at(SPE_NOVEL, ...)`, which is genuinely dead: `mksobj()` reads it only inside `mksobj_init()`, which `init` false skips, and a novel is not `oc_unique`. The site records this. `svc.context.tribute.bookstock` is set but not persisted between segments, unobservable while `mkshop()` makes at most one shop per level and no recorded descent reaches a third. Full suite, six generated-data checks and `check:namespace-members` pass; the review gate is clear with two areas at the advisory checkpoint. CI reports `success`. |
| `43445e9` | 2026-08-01 | behavior slice closure | `488 shown + 194 hidden = 682 total` | `makemon.c set_mimic_sym()`'s `rt >= SHOPBASE` arm (2467-2486), so a mimic generated inside a D:2 shop takes its disguise instead of stopping the descent. **The development score is unchanged in both metrics, 488 of 7,765 screens and 106,404 of 610,816 random-number values, measured by the orchestrator with `npm run checkpoint`** — nothing moved in either direction, which is what this goal's shop work has predicted throughout, since no development session stops at `mkshop()`. **This slice was taken out of the queued order, and the reordering was measured rather than argued.** A correctness pass had deferred the arm; slice 6 then made it the largest remaining shop stop, so it went ahead of slice 7's bookstores. The one-third estimate behind that decision was rechecked rather than inherited: over 8,500 fresh seeds, 37 of 112 D:2 shops of a stocked type held a mimic, 33.0%, matching the closed form `1 - 0.98^n` for a shop of n stocked squares. A first sample of 25 read 24%, so treat 33% as approximate. Evidence is `scripts/run-shop-mimic.mjs`: strict `runFreshMatrix()`, 16 segments, two characters, 91,363 random-number calls and 359 screens, all matching. **Uncertainty, and one coverage hole worth naming.** Two of C's four exits stay unported and refuse by name, a negative `iprobs[]` itype and the health food store's `VEGETARIAN_CLASS`; both belong to rows `SUPPORTED_SHOPS` refuses outright, and the second would draw an `rn2(2)` no consumer spends, which `AGENTS.md` forbids preparing. Slice 8 must delete both. Three mutants survive `--whole-suite` on the `FODDERSHOP` test and the reroll's second clause, all because no `shtypes[]` row lists an itype equal to `MAXOCLASSES` and the one row above it is claimed by that arm, so the operands are only ever true together; a comment records this. And the `map_object()` recolour defect under `## Unresolved` cost coverage here rather than only a seed: 7411559 was the sole jewelers-or-hardware candidate on the stock arm across three scans, so that pairing is exercised by no recorded case. That seed also gave the defect a second independent reproduction — a potion drawn in colour 8 where C draws 6, on a D:1 walk with no descent. `js/makemon_create.js` now imports from `js/shknam.js`, which already imports from it; the repository holds 26 such two-cycles and `get_shop_item` is a hoisted declaration, so it resolves. Full suite, six generated-data checks and `check:namespace-members` pass, gate clear, CI `success`. |
| `dd3c01f` | 2026-08-01 | behavior slice closure | `488 shown + 194 hidden = 682 total` | The descent goal's second shop slice, closed at `fdab09e` with the ratchet decision at this head: the liquor emporium, jewelers and hardware store stock on D:2, with `nameshk()`'s `shktools` arm from `shknam.c:518-520`. Almost nothing needed porting — `get_shop_item()` already walked a three-entry `iprobs[]` and `mkobj_at()` already made potions, rings, gems, amulets and tools — so the slice is mostly `SUPPORTED_SHOPS` admitting three more rows. **Screens hold at 488 of 7,765 and matched random-number values fell 106,408 to 106,404 of 610,816, measured by the orchestrator with `npm run checkpoint`.** That fall is the interesting number, and it is not a regression. `seed0030-ten-diverse-deaths` lost 4 matches, and the orchestrator measured both sides rather than accepting the worker's account: replaying the session against `js/shknam.js` from `44918b8` and then from `fdab09e`, the first divergence sits at random-number index 6336 in both, on the same call, `rn2(5)` at `distfleeck(monmove.c:538)`, with the session's screens and cursors unchanged at eight. What moved is the port's own output past that stop — 34,353 emitted calls became **35,762**, a gain of 1,409, because three more shop types now generate and stock — and that reshuffles the coincidental positional alignment after the stop. `rngMatched` compares positionally over the whole log rather than as a prefix, so a matched count can fall while correctness rises; it rose here. The ratchet was lowered with that measurement recorded beside it. The slice's real evidence is `scripts/run-shop-descent.mjs`, strict. **Uncertainty.** One mutation survivor remains in the slice's own diff, `js/shknam.js:135:31`'s `<` in `nameshk()`'s duplicate-name retry, which needs two shopkeepers on one level; `mkshop()` makes at most one shop and `nameshk()` is not exported, so no path reaches it. The `map_object()` recolour defect recorded under `## Unresolved` did **not** fire: two matrix segments stock gems and both match cell for cell, which narrows that defect's reachable surface. Two findings came out of the slice and are recorded in `ROADMAP.md`: `set_mimic_sym()`'s unported shop arm now stops roughly a third of all shop levels, making it the largest remaining shop stop, and `js/weapon.js:264-268` throws a bare `TypeError` rather than a fail-closed class, which crashes a segment instead of ending it, on an ordinary D:1 walk in about one seed in twelve thousand. The full suite, six generated-data checks and `check:namespace-members` pass; `npm run quality` reports the review gate clear. CI reports `success`. |
| `3d8858cb9315c6bc56259955a46356f322c00712` | 2026-08-01 | behavior slice closure | `488 shown + 194 hidden = 682 total` | The descent goal's shop slice: `mkroom.c mkshop()` from the `!sroom->rlit` lighting loop through `needfill = FILL_NORMAL`, with `shknam.c shtypes[]` generated whole by `scripts/generate-shtypes.mjs` and checked by a sixth generated-data check. A hero descending to a D:2 whose room passes the shop search now walks into a stocked general store, armor shop or weapon shop with its keeper by the door; the other nine `shtypes[]` rows fail closed by name. **Screens are unchanged at 488 of 7,765 and matched random-number values rose 106,354 to 106,408 of 610,816, measured by the orchestrator with `npm run checkpoint`.** All 54 calls are in `seed0030-ten-diverse-deaths`, 6,434 to 6,488, with its screens unchanged at eight — the one development session that reaches `mkshop()`, and only in a segment after its first stop, exactly as the slice-selector predicted. The slice's real evidence is therefore `scripts/run-shop-descent.mjs`, which passes strictly at 12 segments, 66,538 random-number values, 212 screens and 212 cursors. **The instrument this slice repaired matters more than its 54 calls.** `UnsupportedSpecialRoomError` was absent from `js/jsmain.js`'s fail-closed boundary list, so a D:2 asking for a shop escaped `runSegment()` as a crash and discarded the segment rather than keeping its matched prefix; `node scripts/scan-debt.mjs`, which `.agents/selection.md` uses to select every goal, failed outright at `f042aee` and runs again here. Slice 3 introduced that regression by making `mkshop()` reachable for the first time. **Three corrections and one new defect.** The "about half of reachable D:2 levels" figure this tracker and `ROADMAP.md` both carried is wrong: `makelevel()`'s shop arm ends in `rn2(u_depth) < 3`, so at depth two it fires on *every* level with enough rooms, and what varies is whether a room passes `mkshop()`'s search — over 4,000 fresh seeds scanned, 189 descents reached a shop. The general store's table record was genuinely absent rather than present as reported, `js/shknam.js` having held only a `GENERAL_NAMES` copy used as `nameshk()`'s fallback. And `shkinit()` was missing C's two `mongets()` arms, so the port had never spent the `rn2(5)` that `shknam.c:684-687` draws for every general store; the `||` chain short-circuits, which is why the two pre-existing themed shops never exposed it. The new defect is pre-existing and in the display area, not this one: `display.c map_object():340-352` redraws a nearby generic object in its own colour and the port does not, reproduced on **D:1 with no descent at all** by seed 7333427 walking `kkuukkkkk`, where C draws colour 11 for a worthless piece of yellow glass and the port draws 8 with the random-number stream matching exactly. A shop stocking a potion or gem will meet it. **Uncertainty.** 2 of 18 mutants survive, both clauses of the `mongets(SCR_CHARGING)` chain at `js/shknam.js:259-260`, which compare against the refused `shktools`, `shkwands` and `shkrings` and so are false for every shop this slice stocks; slice 6 or 8 kills them. The full suite, six generated-data checks and `check:namespace-members` pass. `npm run quality` fires the review gate here: `generation` reached 1,080 of its 1,000-line limit with 309 generated lines excluded, so a correctness pass is due before further implementation. CI reports `success`. |
| `1cd7c3220fc1db23b360395a6daff9eaf8b596f2` | 2026-08-01 | behavior slice closure, score change | `488 shown + 194 hidden = 682 total` | The descent goal's third slice, and the first level transition this port has ever completed: `do.c goto_level()` 1692-1998 entire, so the hero presses `>` on D:1 and arrives on a D:2 the port generated. **Development rose 487 to 488 of 7,765 screens and 100,910 to 106,354 of 610,816 matched random-number values, measured by the orchestrator with `npm run checkpoint` at this head.** One session accounts for all of it: `seed0015-valk-level2-pit-dog-wait` went from 19 to 20 screens and 2,918 to 8,383 calls, a gain of 5,465, against 21 lost elsewhere. That single screen is a poor measure of the work — the 5,465 calls are the real signal, and they are the first evidence that `js/mklev.js` generates a level correctly against a live game rather than against a test fixture. Ported with it: `savelev()`'s FREEING half in the new `js/save.js` with `save_timers()` and `save_light_sources()`, `losedogs()` and `mon_arrive()` draining the `gm.mydogs` list slice 2 created, hero placement, the transit message, the repaint, the arrival messages, and `pickup(1)` routed through the existing `check_here()` so the queued object-pile goal is not pulled forward. **The boundary was wrong twice before a recording settled it.** `ROADMAP.md` first ended this slice at `docrt()`; the orchestrator corrected that to `goto_level()`'s return; both were wrong. `docrt()` calls `cls()`, whose `display_nhwindow(WIN_MESSAGE, FALSE)` stops for a `--More--` over the level being *left*, with `Dlvl:1` still on the status line, and D:2 appears only once that prompt is dismissed. Every matrix segment now ends with a space. The compensating gain is that `scripts/run-leave-level.mjs` is strict `runFreshMatrix()` again at nine segments, 44,572 calls, 168 screens and 168 cursors, and `scripts/leave-level-matrix.test.mjs`, which existed only to pin slice 2's weaker prefix verdict, is deleted. **Three uncertainties.** The largest is coverage, not correctness: `mkshop()` past its room search is unported, and since `makelevel()`'s shop arm needs only `u_depth > 1`, about half of reachable D:2 levels stop there — the descent goal is *not* closed, and no holdout evaluation was run. Second, 47 of 143 mutants survive after one whole-suite escalation (81 to 64 to 47), clustered on `makelevel()`'s depth chain and on `goto_level()` guards whose two answers reach the same refusal; the per-cluster reasons are in `.agents/implementation-checklist.md`. Third, `u_collide_m()` is connected but has no recorded case and is not exported, so nothing pins it. `seed0030`'s ratchet was lowered at this commit for 21 coincidental positional matches lost after an unchanged stop at random-number index 6336, with screens held at eight; `.agents/validation.md` records that `rngMatched` compares positionally over the whole log, which is why that lowering is sound. The floor was raised over the new gains at `67cd614`. The full suite, five generated-data checks and `check:namespace-members` pass; `npm run quality` reports the review gate clear with three areas at the advisory checkpoint. CI reports `success`. |
| `bb4ef50774898600d8748f5d643b72b3392076ce` | 2026-08-01 | behavior slice closure | `487 shown + 194 hidden = 681 total` | The descent goal's second slice: `do.c goto_level()`'s opening phase — the guards, the context discard at 1601-1622, `keepdogs(FALSE)` at 1624 and `vision_recalc(2)` at 1631 — carrying `dodown()`'s tail (1242-1292) and nine helpers across seven C files with it, among them `dungeon.c next_level()`, `apply.c next_to_u()` in the new `js/apply_next_to_u.js`, `lock.c maybe_reset_pick()`, and `wizard.c mon_has_amulet()` in the new `js/wizard.js`. `dog.c keepdogs()` splits `mon_leave()` and `mon.c relmon()` back out of `js/dog.js migrate_to_level()`, which had folded all three into one function, so the follow arm and the migrate arm share one copy of each as C does. **The development score is identical to the `5b8061d` row, session by session: 487 of 7,765 screens, 487 of 7,765 cursors and 100,910 of 610,816 random-number values, measured by the orchestrator with `npm run checkpoint` at this head rather than taken from the worker.** No development session reaches a down staircase before an earlier boundary stops it, so this slice was never going to move the development score, and its evidence is five fresh C recordings in `scripts/run-leave-level.mjs`: four heroes walk to their level's down staircase and press `>`, and a fifth walks one of those paths without pressing it. Each descending segment matches C value for value and cell for cell through the `>` and then stops, and C's first unmatched random-number call is `getbones(bones.c:645)`, inside the destination-building tail that slice 3 owns. The control segment matches strictly, which is what attributes the one missing screen to the descent alone. **Two uncertainties are larger here than the matching prefix suggests.** First, this is the first matrix in the repository to accept a segment the port does not finish: C draws D:2 after `>` and the port stops one screen short, so `runFreshMatrix()` fails every descending segment and the runner asserts a prefix instead. `scripts/leave-level-matrix.test.mjs` pins that weaker verdict against nine result shapes it must reject, three of them confirmed by breaking the line they cover, but a prefix oracle cannot see a divergence that lands after the refusal. Reduce it to `runFreshMatrix()` when slice 3 lands. Second, `mutate-sites` rewrote every operator and boolean on the changed lines and 2 of 66 mutants survive: the destination clamp's `>` against `>=` at `js/do.js:222`, where the two differ only when `dunlev(newlevel)` already equals `dunlevs_in_dungeon(newlevel)` and the mutant clamps that value to itself, and `reset_utrap(false)` at `js/do.js:313`, whose `msg` arm needs Levitation or Flying that `dodown()`'s own levitation arm refuses first. Both reasons are recorded beside the rows in `.agents/implementation-checklist.md`. Statement coverage, which mutation does not measure, was confirmed separately by deleting each call in the discard and watching the focused tests fail. The full suite, the five generated-data checks and `check:namespace-members` pass at this head; `npm run quality` reports the review gate clear and one area, `startup`, at the advisory checkpoint. CI run 30691126702 reports `success`. |
| `6d7aedd4c83068380821985fb833c83131aba464` | 2026-07-31 | behavior slice closure | `487 shown + 194 hidden = 681 total` | The descent goal's first slice, preserved late: this row was written at `bb4ef50` after the omission was noticed, so its figures are read from the commit and from the checklist's recorded validation at `53ed46c` rather than measured afresh at this SHA. `do.c dodown()` (1129-1294) is ported as far as its refusal arm, so a hero who presses `>` where there is no way down is told `You can't go down here.` and keeps her turn, where the port previously refused the keystroke at the command admission boundary in `js/cmd.js`. Five arms stop rather than run, each with the source reason at the throw, and four helpers come with it: `hack.c u_rooted()`, `steed.c stucksteed()`, and `trap.c uteetering_at_seen_pit()` and `uescaped_shaft()`, whose inlined copies inside `js/engrave.js can_reach_floor()` now call the ported pair. `cmd.c set_move_cmd()` gains a named port, consolidating a second inlined copy in `js/cmd.js executeMovement()` that had dropped the `iflags.menu_requested` arm. **The development score is identical to the `5b8061d` row at 487 of 7,765 screens and 100,910 of 610,816 random-number values, because no development session presses `>` where the refusal answers.** Evidence is `scripts/run-descend-refusal.mjs`, whose eight segments match fresh C recordings on every screen, cursor and random-number call: an ordinary room square, the up staircase the hero starts on, a control typing the same keys without the command, a repeated press, a level carrying a pet, autodig, `number_pad`, and the `#down` prompt. The control and the first segment record the same number of random-number calls and differ by one screen, which is the measurement behind the claim that this path draws nothing. Nineteen mutations across the five production files were each observed failing a test before commit, two survivors were killed by added assertions, and `npm run mutate -- --worktree` reports 36 of 36 mutants killed. The uncertainty carried forward is that a refusal path exercised only by fresh recordings has no development session holding it in place, so a later regression here would surface in the matrix rather than in the score. |
| `5b8061d` | 2026-07-31 | goal closure, authorized holdout evaluation | `487 shown + 194 hidden = 681 total` | The eating goal closes with all three slices ported and four review passes run, and **the reserved holdout moved for the first time in five goals: 139 of 3,640 screens against 138, with random-number values and sessions unchanged at 30,048 of 182,022 and 1 of 11.** One screen is barely above nothing, and it is the first non-zero carry-over since the closed-door goal. Development rose 484 to 487 of 7,765 screens and 100,892 to 100,910 random-number values: slice 1, the prompt and the `is_edible()` refusal, +3; slices 2 and 3, the one-turn meal and the multi-turn occupation, +0 each, because no development session's first boundary is owned by `eat` any more and none reaches a meal at all. **This is the first goal selected by `supports` and the comparison with `#ride` is the point.** `#ride` was chosen on `unlocks` alone, supported 82 screens of 7,765, delivered +17 development and carried nothing. `eat` supported 3,073 across 11 of 33 sessions, delivered +3 development and carried +1. The goal with a fifth of the development gain produced the only holdout movement in five attempts, which is what ranking on breadth rather than on immediate yield was meant to buy. One data point, and the honest reading is that it is consistent with the rule and does not establish it. Four passes ran: a full one over `2fea8eb66..9a7aef5` clearing a fired gate (16 raw, 10 confirmed), a light delta over its own fixes (8 raw, 6 confirmed), and the goal-closing pass over the occupation slice (19 raw, 16 confirmed, none rejected). That last found three production defects on 302 lines whose mutation score was 16 of 16, the best of the session: `moveloop_core()` refusing on a nearby monster after the occupation had already ended, which truncated a segment on the last turn of every meal; `fprefx()` hoisting a `Hallucination` test above both nutrition tests; and `done_eating()` clearing `gn.nomovemsg` with a falsy number against a `??` chain. Mutation says whether tests pin the lines that exist and nothing about a line that is wrong. Three findings are deferred to `## Unresolved`, two of them production: `go.occupation` has one writer and four readers that never see it, so `dochugw()`'s `stop_occupation()` is dead and a meal runs on where C prints; and the status line repaints every turn where C gates on `disp.botl`. |
| `28b9df3` | 2026-07-31 | goal closure, authorized holdout evaluation | `484 shown + 194 hidden = 678 total` | The steed-riding goal closes with all three slices ported and two correctness passes run, and **the reserved holdout is unchanged: 138 of 3,640 screens, 30,048 of 182,022 PRNG values, 1 of 11 sessions, 0 replay errors, identical to the last four measurements, digit for digit.** That is the fourth consecutive goal to carry over nothing, and the first selected by `scripts/scan-debt.mjs` rather than by the first-boundary census, so it tests the new rule rather than the old one. Development rose 467 to 484 of 7,765 screens and 100,825 to 100,892 matched PRNG calls: slice 1, `cmd.c getdir()` and the direction prompt, +2; slice 2, `mount_steed()`'s fourteen guards and the `rnd(20)` slip, +7; slice 3, the success arm with `dismount_steed()` and `teleds()`, +8. **The reading that matters is which column predicted what.** The instrument ranks by `advance`, the screens porting an owner earns next, and `#ride` scored 82 there; it delivered 17. The same instrument's `gated` column, the screens a perfect port would lose without the owner, scored `#ride` at 82 of 7,765, about 1%, against `eat` at 3,073 and `down` at 3,515. Sorting by `advance` bought a narrow win in two Knight sessions, and `dog.c:263-268` with `role.c:209` make the Knight the only role that can reach a saddled steed at all. A goal that two of 33 development sessions need is a goal few of 11 holdout sessions can need. Slice 3 also exposed five pre-existing defects that only a live `u.usteed` could reveal, three of them dropped conjuncts invisible to grep: `weight_cap()`'s `u.usteed && strongmonst()` term, the status line's steed `fly` term, and `moveloop_core()`'s second `vision_recalc()`. Two correctness passes ran: the goal's own over `1f0e7d5..ed43517` (17 raw, 11 confirmed, 11 applied at `3f9be36`), and a wider one over `c706db8a6..ed43517` (16 raw, 10 confirmed, 9 applied at `739e957`, 1 deferred), forced because the first range started after every area's frontier and nine files had never been read. The second found the production defect the first could not see: `test_move()`'s obstacle arm silently answering FALSE for four hero-capability branches C handles differently. Mutation over the wider range killed 335 of 470, 71.3%, against 79.5% over the goal alone. The gate and the advisory are both clear. |
| `628d9fe` | 2026-07-30 | goal closure, authorized holdout evaluation | `467 shown + 194 hidden = 661 total` | The trap goal closes with both slices ported and reviewed, and **the reserved holdout is unchanged: 138 of 3,640 screens, 30,048 of 182,022 PRNG values, 1 of 11 sessions, 0 replay errors — identical to the last three measurements, digit for digit.** That is the third consecutive goal to carry over nothing. Development for the goal as a whole rose 459 to 467 of 7,765 screens and 99,980 to 100,825 matched PRNG calls, all of it from slice 1; slice 2, the dart trap, moved no development session at all. Its own prediction failed: `ROADMAP.md` expected it to unblock `seed1500`, whose recorded dart is a miss, but that session stops earlier on `pet cursed-object feedback` with its PRNG prefix unchanged — the census counted which trap types a blocked session *reaches*, not which refusal it stops on *first*, and only the second predicts movement. Ported in slice 2: `trap.c trapeffect_dart_trap()`'s monster arm, `t_missile()`, `thitm()`'s `!strike` arm, and `worn.c find_mac()`, with three private `ARM_BONUS()` copies consolidated into one. Two correctness passes ran over the goal's tail. The first, full, over `b7cc9a3..b51b1d2`: 22 raw, 18 confirmed, 18 applied. The second, light, over `bf96cda..21cea25`: 10 raw, 9 confirmed, 9 applied — widened because `record-review` refused to advance the frontier over `8de4b76`, the *previous* pass's audit-fix commit, whose twenty-five production lines nothing had read. Five of those nine landed on the first pass's own fixes, which settles whether a fix needs auditing. The sharpest was a test that pinned wrong behavior: it made a monster `minvis` to separate two visibility gates, which also makes `canspotmon()` false, so C's `Monnam()` answers `It` where the port answers the species name; the port has no `x_monnam()` `do_it` branch, and that silent divergence is now recorded under `## Unresolved`. `scripts/mutate-sites.mjs`, merged mid-goal, independently corroborated three of the first pass's test findings. Treat the holdout figure as the measurement it is: three goals of development gains that generalize to nothing. |
| `65d64c2d6b3a98387fe9df4c4905768173904eec` | 2026-07-30 | behavior slice closure, score change | `467 shown + 194 hidden = 661 total` | The first slice of the trap goal, and the largest PRNG gain in some time: development rose 459 to 467 of 7,765 screens, 459 to 467 cursors and 99,980 to **100,825** matched PRNG calls, measured at `f75a9da` and `65d64c2` — the first time past a hundred thousand. Both `seed0013` sessions gained, four screens and about 420 calls each, and nothing regressed. Ported: `trap.c mintrap()` (3732-3840), `trapeffect_selector()` (2936), the monster arm of `trapeffect_sqky_board()` (1439-1475), `trapnote()`, `seetrap()`, `floor_trigger()`, `check_in_air()`, `fixed_tele_trap()`, and `mondata.c`'s `mon_learns_traps()` and `mons_see_trap()`, into a new `js/trap_effects.js` — `js/display.js` and `js/startup_a11y.js` both import `js/trap.js`, so a sibling file follows the existing `trap_erode_obj.js` and `trap_water_damage.js` pattern. Both `trap activation` refusals are gone. **The planning clone was not cloning `level.traps`**, so the dry run's `seetrap()` would have spent `trap->tseen` and left the live pass drawing nothing; `planningState()` now clones them. That is the same clone that produced eight silent divergences across the previous goal, and this time the slice found it rather than a review. A second find is worth recording for its oracle rather than its size: `mintrap()` writes `mtrapped = 0` where the port represents that field as `false` everywhere, and writing the number moved seventeen `second-complete-turn` state digests with the PRNG log and every screen unchanged — a representation split that only the digest comparison could have caught. The worker also wrote a comment claiming `m_harmless_trap()` passes the hero to `check_in_air()`, grepped upstream, found only `dotrap()` does, and corrected it before committing; three consecutive commits in the previous goal shipped exactly that kind of unverified claim. Deliberately unported: every hero arm, since only `dotrap()` supplies the hero and it has no consumer; `mintrap()`'s trapped-monster arm, which `assertSimpleActionState()` refuses first; and the other 22 `trapeffect_*` bodies. The fresh evidence is `scripts/run-monster-squeaky-board.mjs`, matching 10 segments, 47,504 PRNG calls and 2,084 screens and cursors. Known limit for the next slice: these games reach `monmove.c distfleeck()`'s monster-flight refusal within about 150 turns, so the dart slice needs short move budgets. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `2f0e55e99a8896d3fd85b3420a9476c98b0f1169` | 2026-07-30 | goal closure, review window closure, holdout evaluation | `459 shown + 194 hidden = 653 total` | The monster-door goal closed with its three slices, three full correctness passes and two delta reviews recorded, so its authorized holdout evaluation ran here. **Development gained 16 screens across the goal and the holdout gained none — the second consecutive goal to carry over nothing.** Development rose 443 to 459 of 7,765 screens and 99,664 to 99,980 matched PRNG calls; the reserved holdout, aggregate only, is unchanged at 138 of 3,640 screens and 30,048 of 182,022 PRNG values, with 1 of 11 sessions passing and 0 replay errors — identical to the figures at `19b8ed1` and `e18f534`. Three different development sessions moved (`seed0004` +3, `seed0015` +6, `seed2600` +7), so this is not one fixture carrying the goal; the unseen sessions simply do not reach these boundaries before stopping elsewhere. `.agents/validation.md`'s generalization-failure protocol is not triggered: the work is source-faithful, three correctness passes and three fresh matrices verify it, and nothing is special-cased to a recorded session. What the result confirms is the observation the goal was opened with — that exploration's high-incidence work is done, and what remains in this milestone is a tail of coincidence-gated boundaries. Two goals and 17 development screens have now produced zero holdout movement, which is stronger evidence for that reading than the argument it was based on. **The goal's defining feature was not its score but its defect profile: eight silent divergences across four slices, none of which raised a refusal or stopped a segment.** Three were PRNG draws C makes that the port skipped — `mdig_tunnel()`'s `rnd(12)`, `maybe_spin_web()`'s `rn2(1000)` and `hides_under()`'s `rn2(5)`, the last found only because a pass was told to hunt siblings of the first two. One was a trap guard lost when `postSimpleMove()` was evacuated out of the boundary file. One was a restore that set a live `vision_full_recalc` it should have preserved. One was `cant_squeeze_thru()` existing for monsters and not the hero. And two were introduced by the fixes themselves, in a three-commit chain on one seam where each commit was wrong in a different way — a short-circuit that inverted `domove_core()`'s order, then a repair half of which was dead code, then a justification asserting the port lacks a `do_attack()` it has. Three consecutive comments in that chain asserted an invariant the tree did not have. The goal also ported `monmove.c postmov()` into `js/monmove.js` for the first time, leaving `mintrap()`'s position at `:1509` open for the trap goal that attaches there, and gave `vision_recalc()` a state parameter so the cloned planning scan owns its own terrain grid and buffers. Validation at this head: all tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory clear; the three door matrices match 6, 8 and 14 segments. The residual audit-fix commit `278bf6c` changes no executable `js/` line, which is the basis for treating the review deadline as met. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and is not this local holdout figure, which covers 11 sessions rather than the official set. |
| `27761926cb6f41e6b236d9b27b9a7ab809c7d608` | 2026-07-30 | behavior slice closure, score change | `459 shown + 194 hidden = 653 total` | The monster-door goal's last slice, and its largest: `hack.c test_move()`'s testdiag arm (1139-1150) and its exit rule (1208-1214), the same terrain the goal's first two slices covered from the monster's side, now refused from the hero's. Development rose 452 to 459 of 7,765 screens, 452 to 459 cursors and 99,971 to 99,980 matched PRNG calls, measured at `dcd2a66` and `2776192`; only `seed2600-wizard-custom-binds` moved, by 7 screens, and nothing regressed. Across the goal that is 443 to 459, sixteen screens — roughly double the single-digit gain the goal was opened predicting, which is worth recording because the prediction was argued carefully and still came in low. **A sixth silent gap surfaced on the way.** `cant_squeeze_thru()` existed only as a monster-only copy in `js/monmove.js`; the hero half was simply absent, so a hero carrying more than 600 weight would have squeezed a corridor diagonal that C refuses, with no message and no stop. It is consolidated into `js/hack.js` with both C branches. Three further C tests had to come along for the refusals to be correct rather than merely present: the obstacle arm at 1011, so the exit rule cannot claim a TREE, SDOOR, SCORR or IRONBARS square C refuses earlier; the tight-diagonal squeeze at 1153-1171; and `worm_cross()`'s first two tests. Both refusals now live inside `test_move()`, with `preflightDomoveDestination()` admitting the step through one shared predicate rather than restating the rules — the implementer notes that dropping the seam check alone would have been wrong, because a trap or object on the destination would then throw where C refuses first. The `## Unresolved` entry's feared missing mechanism did not exist: the wall arm already owned a no-time refusal that paints a frame. Left unported: `block_door()`/`block_entry()`, since `mklev.c:1349` gates every random shop on `u_depth > 1`; the squeeze switch's three messages, all dormant with no polyform, no Sokoban and no pack over `WT_TOOMUCH_DIAGONAL`; and `worm_cross()`'s `wtails[]` walk. The slice also closed the deferred findings it touched — the false rule, copied four times, that every arm of C's door block tests `D_LOCKED` or `D_CLOSED`, and `scripts/monmove.test.mjs`'s in-sight justification — while deliberately leaving two vision comments alone, on the ground that their refusals may still be right but the reason could not be derived and inventing one is worse. `scripts/run-diagonal-doorway.mjs` is the new matrix. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `06a5629b044794642e83fd6127cddc7f942680d0` | 2026-07-30 | behavior slice closure, score change | `452 shown + 194 hidden = 646 total` | A monster with hands now opens the closed door it steps onto: `monmove.c postmov()`'s `doormask == D_CLOSED && can_open` arm whole, through `UnblockDoor`, with `m_move()`'s `can_open`/`can_unlock` (1763-1766) and `pline.c You_hear()`/`You_see()`. Development rose 446 to 452 of 7,765 screens, 446 to 452 cursors and 99,840 to 99,971 matched PRNG calls, measured at `f8598f3` and `06a5629`; only `seed0015-valk-level2-pit-dog-wait` moved, by 6 screens and 131 calls, and nothing regressed. **The slice's stated precondition turned out to be a real vision-subsystem change, about 170 production lines across three files, and the first design for it was wrong.** `UnblockDoor` calls `vision_recalc(0)`, which had been refused in the cloned planning scan because it rebuilds `vision.c`'s live global buffers. The first attempt admitted only doors whose `viz_array` entry is zero, on the argument that `view_from()` never visited them; a focused test disproved it, because the shadow cast reads the run pointers of clear runs it only partly scans, so an unseen door can still change what the hero sees. The settled design gives `vision_recalc()` an `env` naming the state, the COULD_SEE buffer pair and the redraw, and gives the cloned scan its own terrain grid and buffers. The transparency index has no per-state form, so the clone borrows it and `preflightSimpleMonsterActions()` re-derives it from the live map in a `finally` — a seam worth watching, since it is the one piece of the isolation that is restored rather than owned. Two honest limits recorded rather than papered over: `You see a door open.` has no fresh case, because it needs a monster whose square the hero can see but whom the hero cannot spot — `minvis`, which nothing on D:1 sets — so `scripts/monmove.test.mjs` is its only evidence; and the eight-segment matrix does not discriminate the index restore, only the focused test does. The call-site `!Deaf` gate is likewise untestable against `You_hear()`'s own gate until the hero is also Unaware, which is now a sibling test. Left unported and each refusing by its own name: the door-trap, amorphous, unlock and doorbuster arms. `scripts/run-monster-door-open.mjs` is the new matrix. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `005ea20e047e76958b57d78c39ed3136f3ebe1d1` | 2026-07-30 | behavior slice closure, score change | `446 shown + 194 hidden = 640 total` | The first slice of the monster-door goal, and the one that finally gives `monmove.c postmov()` (1454-1705) a home in `js/monmove.js`: `notice_mon()`, both `newsym()` calls, the door block's fall-through for `D_NODOOR`, `D_BROKEN` and `D_ISOPEN`, and the object arm's `mpickstuff()` branch, in source order, with `mintrap()`'s position at `:1509` left open for the trap goal that attaches there next. `postSimpleMove()` in `js/unported_monster_actions.js` is gone, its destination gate widened to the three inert masks. Development rose 443 to 446 of 7,765 screens, 444 to 446 cursors and 99,664 to 99,840 matched PRNG calls, measured at `6c6fe1d` and `005ea20`; two sessions moved and none regressed. **The PRNG half of that gain is two silent gaps the slice found rather than anything it added.** `may_dig()` admits ordinary floor — it rejects only an undiggable wall or tree — so C calls `mdig_tunnel()`, which draws `rnd(12)` before every early return, and the port was dropping that call on every tunneler's move; `maybe_spin_web()` draws `rn2(1000)` for any webmaker. Both are reachable, both are now refusals, and `seed0030-ten-diverse-deaths` gained 18 matched calls and no screens purely from closing them, which is what a PRNG-only correction looks like. Three tests swapped a rock mole for a rust monster or a gnome to keep covering the arms those refusals now guard. Two decisions the C did not settle: the mask refusal stays at the pre-move gate with `postmov()`'s arm as a second line, because a refusal thrown after a live move leaves a half-applied turn and `advanceElapsedTurn()` does not always dry-run; and `ptr` is dropped from the signature because C refreshes it from `mtmp->data` after `mintrap()`. Known gap: `D_BROKEN` cannot be reached from a fresh D:1 case — 300 generated levels produced none — so `scripts/monmove.test.mjs` is its only evidence, and two candidate seeds were dropped for passing either way because their monster was placed on the doorway at generation. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `e18f5340d2ca1ce25399005ba179cd0d3b9ae5eb` | 2026-07-30 | goal closure, review window closure, holdout evaluation | `443 shown + 194 hidden = 637 total` | The pickup goal closed with its three slices and four correctness passes recorded, so its authorized holdout evaluation ran here. **It carried over nothing, and the honest reading is that there was almost nothing to carry.** Development gained 1 screen across the goal, 442 to 443 of 7,765, and 39 PRNG calls, 99,625 to 99,664; the reserved holdout, aggregate only, is unchanged at 138 of 3,640 screens and 30,048 of 182,022 PRNG values, with 1 of 11 sessions passing and 0 replay errors — identical to the figures at `d0bee36`. The goal-selector predicted carry-over above the development rate but below the closed door's sevenfold, on the argument that the boundary fires inside background monster movement at steps 7 and 8 rather than behind a player command. That prediction was wrong, and the reason is visible in the development number rather than the holdout one: a pickup needs a fetchable object within radius 5 and an apport roll, and the sessions that reach the boundary block again almost immediately. `seed0030-ten-diverse-deaths` was the only session to move at all, and it re-blocks on combat. `.agents/validation.md`'s generalization-failure protocol is not triggered — a goal that gained 1 development screen has no result to fail to generalize. What the goal did buy is not in the score. It ported `dogmove.c dog_invent()`'s carry arm, `objnam.c distant_name()`, `mon.c mpickstuff()` and `worn.c m_dowear()`'s non-creation boundary, and it removed three defects that would have produced silent divergence rather than a refusal: a planning dry run that wrote the live discovery ledger and whose writes survived a rejected round; a clone that dropped eight non-enumerable catalog aliases so the dry run read `undefined` where the live pass read values; and `dochug()`'s standard-attack gate testing adjacency where C uses `inrange && !scared`, which let ranged attackers through in silence. It also removed a performance regression the first of those fixes introduced, measured at 6.4 ms per elapsed turn or 80-92% of a scored turn. Four correctness passes are recorded in `QUALITY.json` covering `f826ba53..4d78313`, `5879bed..2fea8eb` and `2fea8eb..c64d350`: 42 raw findings, 38 after deduplication, 27 confirmed, 11 applied, 16 deferred to `ROADMAP.md`, 9 rejected, none unverified. Three consecutive passes found a defect in the planning clone, and every one of those was introduced by the previous cycle's own fix — the clearest signal in this goal, and the reason its last slice spent itself closing deferred findings rather than porting the dormant `minvis` arm. The residual audit-fix commit `14b47f0` changes no `js/` code at all, which is the basis for treating the review deadline as met. Validation at this head: all 1,925 tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass, with the gate and advisory both clear; `run-monster-pickup.mjs` matches 9 segments and 156 screens, `run-pet-pickup.mjs` 13 segments and 63 screens. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and is not this local holdout figure, which covers 11 sessions rather than the official set. |
| `c64d35015524984772317b67783e3cc59228cec3` | 2026-07-30 | behavior slice closure | `443 shown + 194 hidden = 637 total` | The pickup goal's last slice, and the one that did not close as written. Its subject, `monmove.c postmov()`'s `minvis` redraw (1683-1687), is three lines and dormant: no D:1 case can reach it. `pm_invisible()` covers only the stalker and black light, at difficulty 9 and 7 against `monmax_difficulty()`'s cap of 1 at experience level 1; no `dat/` Lua names one, no `mkclass()` caller passes `S_LIGHT`, and `MM_MINVIS` is wizard-mode only. The two routes C really does take at level 1 — a worn cloak of invisibility at `worn.c:598` and a quaffed potion through `muse.c use_misc()` — are each refused earlier as `monster equipment changes` and `monster item use`. So the arm is dormant rather than silently wrong, which is why it is recorded under `## Unresolved` and left for whichever refusal lifts first, rather than ported against a case that cannot exist. **The slice's actual content became the deferred findings from the two preceding correctness passes**, nine of them closed and each confirmed by breaking the line it covers and watching the test fail. The sharpest was `carryamt`: `select_postmove_object_action()` reports it to `mpickstuff()`, and reporting the full stack instead would empty a floor square C leaves a remainder on, print a different quantity, and drop one `rnd(2)` draw — desynchronising every call after it — with neither the suite nor the differential noticing. It needed a unit test rather than a matrix segment, because every nohands taker is difficulty 4 or above and unreachable on D:1. Also closed: `dochug()`'s `!noattacks()` gate term, `panicattk`'s `range.scared` condition, the redraw running when the hero cannot see the square in both `mpickstuff()` and `dog_invent()`, `distant_name()`'s `oartifact` disjunct, and the planning clone's object-remap arms for lights and timers. Two facts corrected along the way: a gnome is `M2_GNOME | M2_COLLECT`, not `M2_GREEDY`, and `likes_objs()` does not cover `COIN_CLASS`, so no gnome would take gold at all — three fixtures now use a dwarf. The development score is unchanged at 443 of 7,765 screens, 444 cursors and 99,664 PRNG calls, which is the expected result for a slice that added tests and no reachable behavior. `scripts/run-monster-pickup.mjs` matches 9 segments, 27,441 PRNG calls, 156 screens and 156 cursors; `run-pet-pickup.mjs` matches 13 segments and 35,300 calls. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `4d78313de6e5ae5735c531bba8982b089bd2ac00` | 2026-07-30 | behavior slice closure, score change | `443 shown + 194 hidden = 637 total` | The first slice of the pickup goal: a pet lifts an object off the floor square it stands on. `dogmove.c dog_invent()` 443-474's carry arm, `objnam.c distant_name()` 347-409 with the `gd.distantname` read at `xname_flags():627`, and `mon.c check_gear_next_turn()`. Development rose 442 to 443 of 7,765 screens, 443 to 444 cursors and 99,625 to 99,664 matched PRNG calls, measured at `2d5c315` and `4d78313`; only `seed0030-ten-diverse-deaths` moved and nothing regressed. **The slice turned out to be much larger than naming, and the reason is worth recording: it is the first admitted monster action that writes to an object.** `preflightSimpleMonsterActions()` dry-runs the whole monster scan on every elapsed turn, and `planningState()` cloned monsters, lights and timers while sharing `level.objects`, `objlist` and every object instance — so the dry run emptied the live square and the live pass then found nothing there. Deferring the effect during planning is not available either, because the clone's PRNG would diverge from the live pass. The fix is `cloneFloorObjects()` in `js/unported_monster_actions.js`; monster inventories and hero belongings stay shared, with the `assertSimpleActionState()` refusal that keeps them unreachable named at the site. Slice 3's split-stack arm landed here rather than later, because `can_carry()` caps a nohands pet at one item, so every gold pickup splits and 20 of 36 verified fresh cases hit it — refusing it would have been code deleted immediately. Two decisions the C did not settle: `distant_name()`'s `o_id` save and restore is dead, since `preflightObjectName()` refuses `program_state.gameover` first, and the counter decrement is a `finally` because a JavaScript formatter refuses by throwing. The fresh evidence is `scripts/run-pet-pickup.mjs`, matching 13 segments, 35,300 PRNG calls, 63 screens and 63 cursors. Known gap: no fresh case covers a pickup on a square the hero cannot see; 800 seeds produced none, and `scripts/dogmove-inventory.test.mjs` is its only evidence. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `d0bee36028c22cea7435423e94b122c2d296fbf6` | 2026-07-30 | goal closure, review window closure, holdout evaluation | `442 shown + 194 hidden = 636 total` | The closed-door goal closed with all three slices and their three correctness passes recorded, so its authorized holdout evaluation ran here. **This is the first goal whose holdout gain exceeded its development gain outright, and by a wide margin.** Across the goal, development rose 433 to 442 of 7,765 screens and 99,552 to 99,625 matched PRNG calls; the reserved holdout, aggregate only, rose 117 to 138 of 3,640 and 29,959 to 30,048, with **1 of 11 sessions now passing where none did before** and 0 replay errors. The holdout gained 21 screens against development's 9 while holding a third as many sessions: +1.91 per session against +0.27, a factor of seven, or a factor of five normalizing by screens available instead. Both earlier goals ran the other way — search carried at about half the development rate, the extended-command prompt at two-thirds, and running at nothing — so the development set understated this work rather than overstating it, which is the failure mode `.agents/validation.md`'s protocol does not cover and does not need to. The likely reason is prosaic and worth recording: a closed door is something a hero meets in the first few steps of most levels, whereas `s`, `#` and a run command are reached only by sessions that get that far and choose them. The goal ported `hack.c test_move()`'s `IS_DOOR && closed_door()` arm and `lock.c doopen_indir()` into a new `js/lock.js`: the `rnl(20)` pull with both outcomes, the `!(doormask & D_CLOSED)` message switch, and the arms that run when the autoopen test fails, including `Ouch!  You bump into a door.` and its inverted bookkeeping. It also discharged the `blocksMove()` wrong-door-field debt and, along the way, `hack.c move_out_of_bounds()`, after an off-map accident turned out to be load-bearing. Three full correctness passes are recorded in `QUALITY.json`, covering `d1a71f7..5879bed`, `5879bed..cdb83a4` and `cdb83a4..4d54fa8`: 50 raw findings, 46 after deduplication, 39 confirmed and all 39 applied, 7 rejected with counter-evidence, none unverified. Four were live production defects, and none of them moved a development session — an unregistered boolean option whose `autoopen:false` stored a truthy string, a run mode read before the seam assigned it, a portcullis guard refusing a superset of the C predicate, and a `D_LOCKED | D_TRAPPED` door refused where C simply names it. The residual audit-fix commit `1dc06e8` changes no executable code: every `js/` line in it is a comment, which is the basis for treating the review deadline as met. Validation at this head: all 1,883 tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass, with the review gate and advisory both clear; `scripts/run-closed-door-autoopen.mjs` matches 37 segments, 98,012 PRNG calls, 111 screens and 111 cursors. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and is not this local holdout figure, which covers 11 sessions rather than the official set. |
| `4d54fa81d05cc763102b5a17dfc44125325ce46b` | 2026-07-30 | behavior slice closure | `442 shown + 194 hidden = 636 total` | The third and last slice of the closed-door goal: `hack.c test_move()` 1099-1128, the arms that run when the autoopen test at 1097 fails. A hero who walks orthogonally into a door she cannot open gets `Ouch!  You bump into a door.` with `exercise(A_DEX, FALSE)`, or `That door is closed.`, and a diagonal step falls through silently. **The development score is identical to the `cdb83a4` row, session by session, because no development session reaches this arm, so the fresh matrix is the whole of the evidence:** `scripts/run-closed-door-autoopen.mjs` grew by 18 segments to 37 and matches 98,012 PRNG calls, 111 screens and 111 cursors. This arm inverts slice 1's bookkeeping — `hack.c:1122-1127` sets `svc.context.door_opened = svc.context.move = TRUE` and calls `nomul(0)` by hand, because C has just claimed a move it did not make and has to stop a run itself. The `move = TRUE` half turned out to be unobservable, since `rhack()` has already set it, and the code says so where it sits rather than pretending a test covers it. Three refusals had to move above the arm's early return rather than below it: `u.usteed`, a monster on the door square, which `domove_core()` attacks at 2786-2796 first, and `u.utrap`, which `domove_core():2830` hands to `trapmove()` — missing that last one would have admitted a held hero. Blindness, Confusion and Stunned stay refused, the latter two under a new reason, because `domove_core()` reroutes an impaired step through `impaired_movement()` at 2425 before `test_move()` runs and draws `u_maybe_impaired()`'s `rn2(5)`. This head also discharges the `blocksMove()` wrong-door-field debt `ROADMAP.md` had routed to this goal: slice 1 deleted the term rather than correcting it, so the entry is deleted too. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass, with the review gate and advisory both clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `b8323b12c1bb43468c60a899ccec4123dd659932` | 2026-07-30 | review window closure | `442 shown + 194 hidden = 636 total` | The correctness pass over the locked-door slice, `5879bed..cdb83a4`, and its fixes. 17 raw findings, 14 after deduplication, 10 confirmed and all 10 applied, 4 rejected with counter-evidence, none deferred or unverified. The score is identical to the `cdb83a4` row, because the one live defect is not reached by any development session. That defect is the reverse of the usual kind — the port refused a case it fully supports. `lock.c:855` tests only `D_CLOSED`, so `D_LOCKED | D_TRAPPED` enters the same message switch as a plain locked door, prints the same line and returns at `:895`; the `b_trapped()` and `add_damage()` tail at `:907-911` sits inside the "known to be CLOSED" arm below that return. `D_CLOSED | D_TRAPPED` stays refused, because its roll really does reach the tail. The second production finding is a premise three comments shared and the C refutes: `monmove.c closed_door()` does not admit `D_LOCKED` and `D_CLOSED` alone, being the bit test `doormask & (D_LOCKED | D_CLOSED)`, so the seam's mask guard is not the restatement those comments made it look like but the only thing keeping a trapped door away from the pull. Six test findings were assertions that could not fail: two of `hack.c:1097`'s four autoopen terms had no case at all, the drawbridge predicate's vertical arms were left unguarded when the range replaced a four-way adjacency check with it, an inventory fixture prepended its tool so the walk was never exercised, a whole-mask witness landed on the same arm either way, and the replay oracle read retained message history rather than the rendered line. Every new or changed test was observed failing against a named mutation, each listed in the fix commit. Validation at this head: all tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory both clear; `scripts/run-closed-door-autoopen.mjs` matches 19 segments, 50,018 PRNG calls, 61 screens and 61 cursors. Audit metrics and all four rejections are recorded in `QUALITY.json`. The fix commit is itself correctness debt for the next pass. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `cdb83a481180205a8f011ee67b066116a93aaad6` | 2026-07-30 | behavior slice closure, score change | `442 shown + 194 hidden = 636 total` | The second slice of the closed-door goal: `lock.c doopen_indir()`'s `!(doormask & D_CLOSED)` message switch (855-896), so a hero who walks into a locked door is told `This door is locked.` Development rose 436 to 442 of 7,765 screens, 437 to 443 cursors, and 99,541 to 99,625 matched PRNG calls, measured at `5b738a7` and `cdb83a4`. One session moved and none regressed: `seed0004-feeding-pony` took all six screens and all 84 calls. The locked arm draws nothing itself — a hero holding no skeleton key, lock pick or credit card gets `autokey(TRUE)` returning null and the `flags.autounlock` block does nothing — so the gain is the turn continuing rather than the message costing randomness. Only one of the switch's four arms is reachable this way: `monmove.c closed_door()` answers TRUE for `D_LOCKED | D_CLOSED` alone, so the broken, doorless and already-open arms arrive solely through the unported `#open`. The switch is one C statement and was translated whole, which is what `AGENTS.md` prefers to splitting a source construct, but it leaves three arms that write output with unit tests as their only evidence; `ROADMAP.md` records that and the narrower refusal on an explicit `autounlock` setting. The fresh evidence is `scripts/run-closed-door-autoopen.mjs`, grown by a nine-segment locked recipe to 19 segments, 50,018 PRNG calls, 61 screens and 61 cursors. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass at this head, with the review gate and advisory both clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `cfbc2055ad2db10598193f9c75f9a69bbd5cd4e7` | 2026-07-30 | review window closure | `436 shown + 194 hidden = 630 total` | The correctness pass over the closed-door slice, `d1a71f7..5879bed`, and its fixes. 16 raw findings, 15 after deduplication, 13 confirmed and all 13 applied, 2 rejected with counter-evidence, none deferred or unverified. The score is identical to the `5879bed` row, call for call and screen for screen, because none of the three live production defects is reached by a development session — which is why the new tests are the gate here rather than the score. Those three were each reachable by ordinary input: `flags.autoopen` was never registered in `HANDLED_BOOLEAN_OPTIONS`, so `autoopen:false` stored the truthy string `"false"` and the port pulled at a door C refuses; `requireAutoopenClosedDoor()` read a `state.context.run` that `js/cmd.js` assigns only *after* calling the admission preflight, so a rush into a closed door committed its movement intent before being refused; and the portcullis guard re-implemented `dbridge.c is_drawbridge_wall()` without its `DB_DIR` test, refusing a door whose neighbouring drawbridge faces away, which C opens normally. Five further findings were assertions that could not fail — a refusal matrix covering one of six terms, a replay oracle checking only a screen count, and a folded-Strength case satisfied by any path that left the door shut. Every new test was observed failing against a named mutation, each listed in the fix commit. Validation at this head: all tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass with the gate and advisory both clear; `scripts/run-closed-door-autoopen.mjs` matches 10 segments, 27,136 PRNG calls, 33 screens and 33 cursors, and the rush matrix 32 segments and 100,341 calls. Audit metrics and both rejections are recorded in `QUALITY.json`. The fix commit is itself correctness debt for the next pass. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `5879bed` | 2026-07-30 | behavior slice closure, score change | `436 shown + 194 hidden = 630 total` | The first slice of the closed-door goal: a hero who walks into a plain `D_CLOSED` door now opens it or is resisted, through `hack.c test_move()`'s `IS_DOOR && closed_door()` arm and the `autoopen` route into `lock.c doopen_indir()` in a new `js/lock.js`. Development screens rose 433 to 436 and cursors 433 to 437, measured at `1144d29` and `5879bed`. **Matched PRNG calls fell, 99,552 to 99,541, and that is an artifact of the metric rather than a regression — worth understanding before anyone reads a future dip as one.** `rngMatched` in `frozen/ps_test_runner.mjs:356-358` compares the two logs *positionally* over their whole length, not as a prefix, so a session whose port run stops early has its next segment's startup calls scored against C's still-running segment. Moving that stop by three calls reshuffles which downstream calls coincidentally line up. The correctness measure is the prefix, and it improved: on `seed0030-ten-diverse-deaths` the port diverged at call 6305 at the parent, exactly where C draws `rnl(20) @ doopen_indir(lock.c:904)`, and diverges at 6308 here, having made all three door draws — `rnl(20)=16`, `rn2(19)=10`, `rnl(20)=10`, a resist then an open — exactly as C does. That session gained 2 screens and `seed0077-rogue-chargen` 1; no session lost a screen. Two findings came from the work rather than from a recording. Taking over `domove()`'s call site exposed an accident older than this slice: `blocksMove()` answered "blocked" for a square off the map, which is how a step past the edge had been refused, and the fix is `hack.c move_out_of_bounds()` ported whole, with its `mention_walls` and forcefight arms; the corridor-run and rush-run matrices caught it on seed 6100003. And `blocksMove()`'s door term is deleted rather than corrected, the debt `ROADMAP.md` had routed to this goal: once `closed_door()` owns the route in both callers a corrected term would be unreachable. `svc.context.door_opened` and `flags.autoopen`, whose `opt_out` default comes from `optlist.h:181`, are new state, so all 17 `second-complete-turn` digests changed; a full snapshot diff against the parent confirmed those two fields are the only difference and that matrix still matches fresh C on all 17 segments. Deliberately unported and refused before any draw: a blind hero, because no case in this slice can validate `feel_location()` on a door; every polymorph form; `Underwater`; `u.utrap` and `u.usteed`; mimics; portcullises; and every mask but plain `D_CLOSED`. The fresh evidence is `scripts/run-closed-door-autoopen.mjs`, matching 10 segments, 27,136 PRNG calls, 33 screens and 33 cursors. All tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass at this head, with the review gate and advisory both clear. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `19b8ed124ad2a749ff28415cd875c9f8687c79c7` | 2026-07-30 | goal closure, behavior slice closure, review window closure, holdout evaluation, score change | `433 shown + 194 hidden = 627 total` | Two goals closed here: the explicit search command, and the repeated-simple-commands goal, which had been behaviorally complete since `4fc57d8` and was waiting only for the correctness debt its fix tail carried. **The search work carried over at about half its development rate.** Development gained 35 screens, 398 to 433 of 7,765, and 56 PRNG values, 99,496 to 99,552; the reserved holdout, aggregate only, gained 6 screens, 111 to 117 of 3,640, and 8 PRNG values, 29,951 to 29,959, with 0 of 11 sessions passing and 0 replay errors. Per session that is +1.06 screens on development against +0.55 on the holdout, so roughly half the rate reached unseen seeds — between the extended-command goal's two-thirds and the running goal's nothing, and consistent with `s` being reached by some sessions but not most. That 51% is the more generous of two defensible normalizations and the figure to quote sceptically: holdout sessions are half again as long, at 331 recorded screens each against development's 235, so dividing by screens available rather than by session gives 0.165% against 0.451%, a carry-over of 37%. Per-session is used here only because the rows above use it, which keeps goal-to-goal comparison meaningful; the honest range is 37 to 51%. `.agents/validation.md`'s generalization-failure protocol is not triggered, because both sets gained. Five development sessions improved and none regressed, and `seed8000-tourist-starter` became the first session to match completely, at 23 of 23. The port covers `detect.c dosearch()` 2095-2104, the `aflag == 0` arms of `dosearch0()` 2015-2093, `mfind0()` 1964-2013 for `via_warning == 0`, and `display.c unmap_invisible()` 387-396. Its one design constraint is that `dosearch0()` draws `rnl()` per adjacent square inside its loop, so every unported case is refused over the whole 3x3 before the first draw, unlike the pre-existing `aflag == 1` owner which refuses inside the loop after the source draw succeeds. `mfind0()`'s three discovery arms and `unmap_invisible()`'s TRUE arm are excluded and refuse before any draw; they need `map_invisible()`, which needs a canonical glyph identity the port's map memory does not carry. Two correctness passes are recorded in `QUALITY.json`. The full pass over `e30ea05..d1a71f7` claimed eight areas from the oldest frontier among them, which cleared every advisory checkpoint and absorbed `88e2193`, the previous cycle's audit-fix commit: 13 raw, 13 deduplicated, 9 confirmed, 7 applied, 2 deferred to `ROADMAP.md`, 4 rejected with counter-evidence, none unverified. Its two reachable production defects were `M_AP_TYPE()` dropping `monst.h:73`'s mask, so a monster carrying only `M_AP_F_DKNOWN` was refused where C calls `newsym()`, and the statue-trap and hallucinatory-trap refusals throwing a bare `Error` that escaped `runSegment()` instead of converting to the retryable command boundary. Neither moved a development session, which is why the fresh matrix and the new tests are the gate rather than the score. The light delta pass over `d1a71f7..c706db8` then found 5 more, all collateral from those fixes and all applied: the sharpest was that the new end-to-end statue-trap case could not distinguish a refusal decided before the loop from one decided inside it, the single invariant the slice rests on, because no square in that hero's 3x3 draws at all. Validation at this head: 1,857 tests, four generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass, with the review gate and the advisory both clear in every area; `scripts/run-explicit-search.mjs` matches 13 segments, 35,560 PRNG calls, 80 screens and 80 cursors. The residual audit-fix commit `5dd04b2` changes no `js/` code, which is the basis for treating the review deadline as met. Known gap: `dosearch0()`'s SCORR arm has focused tests only, because 28,000 generated levels produced no hero adjacent to a secret corridor. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and is not this local holdout figure, which covers 11 sessions rather than the official set. |
| `ae1a94b0294dba841a4b81e97c914c8327b6fa72` | 2026-07-29 | goal closure, holdout evaluation | `398 shown + 194 hidden = 592 total` | The extended-command prompt goal closed, so its authorized holdout evaluation ran here. **This one carried over, and the previous one did not: development gained 77 screens across the goal and the holdout gained 17.** Reserved holdout, aggregate only: 0 of 11 sessions passing, 0 replay errors, 111 of 3,640 screens against the 94 recorded at `b0de52e`, and 29,951 of 182,022 PRNG values, identical to that run. The flat PRNG total is the predicted signature rather than a null result: a prompt keystroke paints a screen without drawing randomness, which is exactly what development showed when it gained 77 screens at an unchanged 99,496 values. Per session the gain is +2.33 screens on development against +1.55 on the holdout, so roughly two-thirds of the rate carried to unseen seeds; the standing per-session levels are 12.1 matched screens against 10.1, on holdout sessions that are half again as long at 331 recorded screens each against development's 235. `.agents/validation.md`'s generalization-failure protocol is not triggered. The contrast with the running and rushing goal is informative and was predicted at that time: a run command is reached by only a fifth of sessions and late, whereas `#` is reached early by most, so the extended-command work had far more opportunity to land. Reaching this evaluation took four consecutive light delta reviews of the audit-fix tail, recorded in `QUALITY.json`, because each of the first three returned confirmed findings, twice including defects introduced by the previous round's own fix; the fourth found no production defect and its fix touched test files only. The one residual audit-fix commit, `a010008`, changes no `js/` code at all, and every area reports clear, which is the basis for treating the review deadline as met. Validation at this head: 1,824 tests, five generated-data checks, `check:namespace-members` and `npm run quality -- --check` pass, with the review gate clear and three areas at their advisory checkpoint; all four matrices match at 57,676, 87,714, 100,341 and 71,392 plus 12,261 PRNG calls. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and is not this local holdout figure, which covers 11 sessions rather than the official set. |
| `f826ba5392d313da8951095111b8201f5be497fe` | 2026-07-29 | behavior slice closure, score change | `398 shown + 194 hidden = 592 total` | Development matches 99,496 PRNG values, 398 screens, and 398 cursors: 77 screens above the parent `af098f7`, which is the figure the slice was selected on, counted keystroke by keystroke before any code was written. Per session, nine improved, none regressed, and 24 are byte-identical; the nine are exactly the sessions that stop on `#`, gaining 12 screens each for the five `playmode:debug` tours and 2 to 5 for the rest. The PRNG total is unchanged, because a prompt keystroke paints a screen without drawing randomness. The port covers `cmd.c` `extcmd_initiator()`, `extcmds_match()`, `can_do_extcmd()`, `accept_menu_prefix()`, `doextcmd()` and a generated `extcmdlist[]`, with `win/tty/getline.c` `hooked_tty_getlin()` under NEWAUTOCOMP, `ext_cmd_getlin_hook()`, `tty_get_ext_cmd()` and `tty_getlin()` over a `topl.c` painter. The generated table is declared in `QUALITY.json` with its generator and check, which `npm run checkpoint` now runs as `check:extcmds` and which excludes its 207 lines from the review thresholds. Three findings came from tracing rather than from output. `custompline(SUPPRESS_HISTORY)` does not reach `update_topl()`: `putmesg` turns it into `ATR_NOHISTORY` and `wintty.c:119` advertises `WC2_SUPPRESS_HIST`, so the prompt goes through `remember_topl()` and `show_topl()`, and implementing it as a pline would have merged the prompt into a pending top-line message. `##` is legal, because `#` is itself an `extcmdlist[]` row matching under `ECM_EXACTMATCH`, so C recurses; a fresh differential found it. And `seed0102`'s `# name` at column 3, which the roadmap had recorded only as an observation, is what `hooked_tty_getlin()` produces: after `putsyms(bufp)` prints the expansion it emits one backspace per remaining character. All six `#if` regions in the table were resolved by compiling a probe against `nethack-c/recorder/include` rather than by reading the table. The fresh evidence is `scripts/run-extended-command-prompt.mjs`, matching 71,359 PRNG calls with 370 screens and cursors across 31 ordinary segments and 12,261 calls with 33 screens across four debug segments. All 1,808 tests, five generated-data checks and `check:namespace-members` pass at this exact head. `npm run quality` reports the gate BLOCKED, with `commands` at 1,289 of 1,000 unreviewed lines, so a correctness pass over this range is due before further implementation. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `b0de52e2d44db14fd322633e25d365c8b24e95cb` | 2026-07-29 | goal closure, holdout evaluation, review window closure | `321 shown + 194 hidden = 515 total` | The running and rushing goal closed and its correctness pass is recorded in `QUALITY.json`, so its authorized holdout evaluation ran here. **The result deserves stating plainly: development gained 35 screens across this goal, 286 to 321, and the holdout gained none.** Reserved holdout, aggregate only: 0 of 11 sessions passing, 0 replay errors, 94 of 3,640 screens, unchanged from the 94 recorded at `4a655d6`, and 29,951 of 182,022 PRNG values against 29,939, a gain of twelve. That is the shape `.agents/validation.md`'s generalization-failure protocol exists to catch, so the comparison it requires ran first. The source and diff carry no value taken from any recorded session: `git diff 2adc5af..60bf3d0 -- js/` contains no seed, session name, or recorded input, the largest slice was the deletion of a `here.typ !== ROOM` guard, and `lookaround()` was ported whole from `hack.c:3898-4059`. The development evidence points the same way: only three of the 33 development sessions still name a run command in the census, the room-run slice moved 7 sessions, corridor running moved 2, and rush moved none by design. A command that only a fifth of sessions reach, and only after passing every earlier boundary, is expected to move a given set of eleven sessions rarely or not at all. So the protocol is not triggered — this reads as a rare command rather than as fixture-specific behavior. The honest limit is that the reason cannot be confirmed: establishing that no holdout session reaches a run before its own stop would mean inspecting them, which `AGENTS.md` prohibits, so the twelve matched PRNG values are the only direct evidence that any holdout session advanced at all. Validation at this head: 1,800 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass, with the review gate clear and three areas at their advisory checkpoint; all three run matrices match 90,877, 87,714 and 57,676 PRNG calls. Six test-coverage findings from the pass are deferred and recorded in `ROADMAP.md`. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and is not this local holdout figure, which covers 11 sessions rather than the official set. |
| `60bf3d04c84bce48ff7aadf7989556874ee0f3e5` | 2026-07-29 | behavior slice closure | `321 shown + 194 hidden = 515 total` | Development matches 99,496 PRNG values, 321 screens, and 321 cursors, identical to the parent `0e73733` session by session across all 33, which is the intended result: no development session stops on a run boundary, so this slice was never going to move the score and was not judged on it. The production delta is `ADMITTED_RUN_MODES` in `js/cmd.js` admitting 3, plus a corrected comment that had conflated the ctrl-direction keys with the `g` and `G` prefixes. `cmd.c:1461` `do_rush_<dir>` calls `set_move_cmd(dir, 3)`, while `cmd.c:1599` `do_rush()` behind `g` sets 2 and `cmd.c:1606` `do_run()` behind `G` sets 3. The slice's value is that run 3 executes three arms of `lookaround()` that had never run, each traced against `hack.c:3898-4059` and each correct as ported: the monster arm at 3933, where `run != 1 && !is_safemon(mtmp)` stops a rush for a hostile beside the hero rather than only in front, and where a pet alongside correctly does not stop it; the closed-door arm at 3967; and the trap arm at 3950, whose `infront && run > 1` argument becomes true here for the first time. The trap arm is exercised but its TRUE result is not: `avoid_moving_on_trap()` answers TRUE only for a `tseen` trap, and the only D:1 route to one is a themed teleportation hub that the port's seams refuse before a rush reaches it. The script header records that rather than a case being fitted to it. `blocksMove()`'s recorded wrong-field defect did not surface, because `closed_door()` reads a `doorMask()` helper that already handles both fields and stops the rush a square earlier. The fresh evidence is `scripts/run-rush-runs.mjs`, matching 90,877 PRNG calls with 115 screens, 115 cursors and 325 animation frames across 30 segments; the corridor and room matrices still match 87,714 and 57,676 calls. All 1,798 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head, with the review gate clear and five areas at their advisory checkpoint. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `e9cd289b6803c1d2686619af410ae686de531e19` | 2026-07-29 | behavior slice closure, score change | `321 shown + 194 hidden = 515 total` | Development matches 99,496 PRNG values, 321 screens, and 321 cursors, the largest single-slice gain recorded here at 31 screens. The production change is four inserted and fifteen deleted lines in `js/hack.js`: the `here.typ !== ROOM` guard that suppressed `lookaround()`'s corridor arms is gone, and three comments that described the guard are corrected. Everything the slice needed was already ported by the room-run slice, including `domove_core()`'s doorway stop, so the delta was smaller than the slice description implied; the arms were traced line by line against `hack.c:3898-4059` before being trusted, since none had ever executed. Per session against the parent `29082f7`: two sessions improved, none regressed, 31 byte-identical. `seed0017` carries almost all of it, 11 to 41 screens, and `seed0004` gained the single step its locked-door next key allows, which is what the slice selection predicted. The fresh evidence is `scripts/run-corridor-runs.mjs`, matching 87,714 PRNG calls with 126 screens, 126 cursors and 268 animation frames across 27 segments; `scripts/run-room-runs.mjs` still matches 57,676 calls with 61 screens and 163 frames. The diagonal corner-turn collision with `requireNonDiagonalDoorway()` was settled from the C rather than by loosening the check: `test_move()`'s `testdiag` and `ust` arms both hang on `doorless_door()`, which that helper already applies, so a turn onto a `D_NODOOR` doorway passes and the run ends there, and a turn cannot start from a doorway because `nomul(0)` zeroes `multi` first. One coverage limit is deliberate and reduces what this matrix can catch: all 27 segments use human characters, because a race with infravision reaches the recorded `newsym()` defect in `ROADMAP.md`, so the matrix cannot detect a race-dependent regression. All 1,793 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head, with the review gate clear and four areas at their advisory checkpoint. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `bc6b5aaca117f65698667acb5a8512489c1683fb` | 2026-07-29 | behavior slice closure, score change | `290 shown + 194 hidden = 484 total` | Development matches 98,989 PRNG values, 290 screens, and 290 cursors. This is the milestone's first goal whose command consumes game time, so the PRNG total moved for the first time since `03c2add`, by 230 values. That made regression the thing to measure rather than the gain: a per-session comparison against the parent `bdeed27` shows **seven sessions improved, none regressed, and 26 byte-identical**, and the seven are exactly the ones that stop on a run command. `hack.c lookaround()` is ported whole, with `domove_core()`'s two run arms, `nomul()`, `runmode_delay_output()`, `avoid_moving_on_trap()`, `avoid_moving_on_liquid()`, `pickup.c check_here()`'s run stop, and `allmain.c interrupt_multi()`. Corridor running is refused inside `lookaround()` at `levl[u.ux][u.uy].typ != ROOM`, the single source test every corridor arm hangs off. The fresh evidence is `scripts/run-room-runs.mjs`, matching 57,676 PRNG calls with 61 screens, 61 cursors and 163 animation frames across 19 segments; animation frames were an unvalidated stream before this slice and are now compared by `scripts/diff-fresh.mjs`, with the three older matrices still passing at zero frames each side. One latent defect surfaced and was fixed rather than worked around: `js/regen.js` threw a bare `TypeError` when the hero reached full hit points with `multi > 0`, unreachable until a run made `multi` positive and a hard failure that would have cost a segment its whole prefix; `interrupt_multi()` is now ported and exempts runs as C does. Two further defects were found, left unfixed, and recorded in `ROADMAP.md`: `newsym()` omits `display.c`'s out-of-sight infrared arm, reproducible at seed 7000063, and `blocksMove()` reads `loc.doormask` where `mklev.js` writes `loc.flags`, which no output depends on today because the `typ === DOOR` arm refuses the square first. All 1,785 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head, with the review gate clear and four areas at their advisory checkpoint. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `525f43f8b4450705f831c98e049e0b8e272769cb` | 2026-07-29 | goal closure, holdout evaluation | `286 shown + 194 hidden = 480 total` | The no-time-command goal closed at `7ef240c`, so its authorized holdout evaluation ran here, the first since the goal opened. Reserved holdout, aggregate only: 0 of 11 sessions passing, **0 replay errors**, 94 of 3,640 screens (2.6%), and 29,939 of 182,022 PRNG values (16.4%). Development at the same head matches 286 of 7,765 screens (3.7%) and 98,759 of 610,816 PRNG values (16.2%). The raw screen percentages differ, but they are not comparable: the holdout's sessions are half again as long, at 331 recorded screens each against development's 235. Normalized per session the two agree closely — 8.5 matched screens against 8.7, and 2,722 matched PRNG values against 2,993 — and the PRNG percentage, which does not depend on session length, is the same to within 0.2 points. Read together with zero replay errors, meaning every holdout session stopped at a fail-closed boundary rather than crashing, this says the work generalizes to unseen seeds: the port reaches about the same depth per session whether or not it has seen the session. It does not say the port is close to finishing one, and no session of either set passes outright. `.agents/validation.md`'s generalization-failure protocol is not triggered, because development results did carry over. The evaluation was preceded by the light delta review at `2adc5af`, recorded in `QUALITY.json`, which cleared the audit-fix debt that a review deadline requires settled; the one residual commit, `acf05a3`, changes comments and tests only and left the development score and the differential matrix identical. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and is not this local holdout figure, which covers 11 sessions rather than the official set. |
| `0b87fd287a759e699e340dca67cc3800b40f9aa1` | 2026-07-29 | behavior slice closure, review window closure, score change | `286 shown + 194 hidden = 480 total` | Development matches 98,759 PRNG values, 286 screens, and 286 cursors. The three screens above the `ff6efb9` row are all `seed8000-tourist-starter`, which rose from 17 to 20 of its 23 and now stops on `s`, the search command, in a later goal's territory; per-session comparison against the parent shows no other session changed and none regressed. They come from the `insight.c doattributes()` slice, which closed at `60bf2c3`: `js/insight.js` ports `enlightenment()`, its line layer, all four sections `BASICENLIGHTENMENT` runs, `weapon_insight()`, `attrval()`, `fmt_elapsed_time()` and `align_str()`, with 16 stops for the conditions a fresh D:1 hero cannot carry. Three source findings corrected what the roadmap had recorded: `from_what()` is gated entirely on `wizard` at `attrib.c:905` and so returns the empty string here, `allmain.c:836` sets `urealtime.start_timing` in `newgame()` without which elapsed time reads as 55 years, and `u_init.c ini_inv_use_obj()` rather than `ini_inv_adjust_obj()` calls `initialspell()`. This head also closes the full correctness pass over `eb7e17e..60bf2c3`: 17 raw findings, 16 deduplicated, 14 confirmed and all applied, 2 rejected with counter-evidence, none unverified, recorded in `QUALITY.json` with each rejection's reopening condition. Its four production defects were reachable by inputs the port already accepts and none of them moved a development session: `Fixed_abil` read as intrinsic-or-extrinsic against youprop.h:385's extrinsic-only macro, a stop on every wielded towel where `obj.h` requires `spe > 0`, a deafness guard that missed `u.uroleplay.deaf` so `OPTIONS=deaf` printed a window C gives a deafness line, and `can_advance()` refusing on `speedy` ahead of the three tests C answers FALSE from. That is why the fresh evidence is the gate rather than the score: `scripts/run-no-time-commands.mjs` matches 71,967 PRNG calls with 158 screens and cursors across 29 segments, and the fixes added nine tests plus `scripts/o_init-discoveries.test.mjs` and `scripts/shk.test.mjs`, including eight hand-computed percentages pinning `percent_success()`'s worn-equipment block and the five encumbrance adjectives that no fresh start reaches. All 1,765 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head, with the review gate clear and three areas at their advisory checkpoint. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `ff6efb9b321f78d9dd1f3a6fa149708467b59c64` | 2026-07-29 | behavior slice closure, score change | `283 shown + 194 hidden = 477 total` | Development matches 98,759 PRNG values, 283 screens, and 283 cursors. This head closes the discovery-and-spell-list slice of the no-time-command goal with `spell.c dospellmenu()`'s `SPELLMENU_VIEW` listing, which `+` reaches once the hero knows a spell; it arrives with `percent_success()`, `spellretention()`, `spelltypemnemonic()`, the `spellev`/`spellknow`/`spellname`/`spellet` accessors, `hacklib.c isqrt()`, and `objclass.h is_metallic()`. The four screens above the `e05118c` row were measured commit by commit in detached worktrees, and none of them is this slice's: `8f56084` took 279 to 280 with the empty spell answer, `8588102` took 280 to 281 by admitting Escape at a command prompt, and `30d2b41` took 281 to 283 with the discovery list. `ff6efb9` itself is score-identical to its parent session by session, all 33 unchanged with none improved and none regressed, because no development session reaches `+` before stopping elsewhere; `scan-stops.mjs` puts 26 of the 33 on the unsupported-command boundary and none on `showspells`. The fresh differential is therefore the whole of the evidence: `scripts/run-no-time-commands.mjs` grew to 21 segments and matches 51,853 PRNG calls with 117 screens and cursors, its new segments recorded for the four roles that start knowing a spell: a Healer with three spellbooks, a Monk with one, a Priest whose two books are drawn from the level, and a Wizard. Its `+` segments set `menu_headings:none`, because the default `[no-color&inverse]` heading indents `Name` behind four inverse spaces that `serialize()` in `frozen/terminal.js` cannot emit; `ROADMAP.md` records that ceiling, which costs twelve recorded screens across nine sessions whatever the port does. Two roadmap claims were corrected against the C: `u_init.c ini_inv_use_obj()` calls `initialspell()`, and a Valkyrie starts with no spellbook. All 1,740 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head, with the review gate clear and two areas at their advisory checkpoint. The range from `03c2add` onward has not had its correctness pass. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `e05118c6cce47f3cb8d139b4728367b6010416c6` | 2026-07-28 | behavior slice closure, score change | `279 shown + 194 hidden = 473 total` | Development matches 98,759 PRNG values, 279 screens, and 279 cursors. The six screens above the `7f4c101` row come from the inventory display, the goal's third slice: `invent.c` `ddoinv()` through `display_pickinv()` now build the menu from the class walk `flags.sortpack` and `flags.inv_order` select, and all three sessions that press `i` display and dismiss it. Reaching it took `doname()`'s `owornmask` suffixes, its container branches, and `eat.c tin_details()`, since every starting pack holds a worn item and some hold a sack or a tin. One defect older than this slice surfaced on the way: `js/tty_menu.js` finished a menu on Space only when the caller declared an empty completion, while `process_menu_window()` finishes on Space at the last page unconditionally, which left the inventory menu on screen; the 107-segment first-command closure matrix, matching 311,439 PRNG calls and 119 screens, is the evidence that startup selection menus still behave after the fix. Seven fresh recordings match C cell for cell, five checked into `scripts/run-no-time-commands.mjs`, which matches 29,459 PRNG calls with 71 screens and cursors across twelve segments. No session regressed: per-session results were compared against the parent, where three improved and thirty were unchanged. All 1,719 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head, with the review advisory reporting three areas at their checkpoint threshold. The range from `03c2add` onward has not had its correctness pass. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `7f4c101ab2e77c2efbad64ecb9466f89465563a8` | 2026-07-28 | behavior slice closure, score change | `273 shown + 194 hidden = 467 total` | Development matches 98,759 PRNG values, 273 screens, and 273 cursors. The four screens above the `2628f43` row come from the second slice of the no-time-command goal, `invent.c dolook()` on `:`. `look_here()` now covers a sighted hero on an ordinary square, so the port prints the terrain feature line, reads the engraving, and describes one object or answers `You see no objects here.`, all without consuming game time. Three preceding commits prepared it: `f5939aa` gave `stairs.c` one home in `js/stairs.js`, `a77d2c7` ported `stairs_description()` and `dfeature_at()` with the generated `CMAP_EXPLANATIONS` table, and `7d089fd` ported `an()`, `vtense()`, and `hacklib.c strcasecpy()`. Each of those three left the development score identical, and `f5939aa` was confirmed identical session by session across all 33 sessions before it took its `Score-identical-with` trailer. The no-time matrix grew to seven segments and matches 18,107 PRNG calls with 49 screens and cursors, covering a staircase, a bare floor, a doorway looked at twice, and a square holding one object; the repeated-simple-command matrix still matches 104,649 calls with 2,541 screens, and the second-turn matrix 46,255 calls with 54 screens. Defaulting `flags.pile_limit` to 5, which `look_here()` reads, moved all 17 second-turn state digests and no other oracle field. All 1,717 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head; the review advisory reports one area at its checkpoint threshold, which does not require a pass. `look_here_single_object()` still holds the blind branch that the walk path uses, and merging it is the next piece. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `2628f43fc89000171f2b439f231457237efc009b` | 2026-07-28 | behavior slice closure, score change | `269 shown + 194 hidden = 463 total` | Development matches 98,749 PRNG values, 269 screens, and 269 cursors. The four screens above the `03c2add` row come from the first slice of the no-time-command goal: a byte bound to no command now reaches `rhack()`'s bad-command path, which prints `Unknown command '<key>'.` and takes no game time, instead of stopping the segment. The session that stopped on its very first keystroke, a Space, now runs to its fifth screen. Digits while `num_pad` is off and the count key while it is on stay outside the slice, because `parse()` routes them into `get_count()` before any command lookup. The new checked-in matrix `scripts/run-no-time-commands.mjs` matches 7,538 PRNG calls with 31 screens and cursors across three fresh recordings, and the two older matrices are unchanged at 104,649 calls with 2,541 screens and 46,255 calls with 54 screens. All 1,709 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `03c2addb5ef05f76d1be619a7363f7f78c0c013e` | 2026-07-28 | goal closure, score change | `265 shown + 194 hidden = 459 total` | Development matches 98,686 PRNG values, 265 screens, and 265 cursors. The eleven screens above the `88e2193` row come from admitting `STAIRS` as a monster destination, the monster-side mirror of the hero admission at `a0d6283`, which closes the goal for monster and pet movement onto stairs. Two development sessions advance past it, one to a run command and one to a pet stepping onto a trap; the third session on that boundary is a monster at a `D_CLOSED` door and stays excluded with closed doors generally. The source evidence for the admission is that `STAIRS` is ACCESSIBLE, so `mfndpos()` and `goodpos()` select it with no stair-specific branch, `postmov()` has none either, and no ordinary movement path calls `migrate_to_level()`. The checked-in repeated-simple-command matrix grew from 17 to 19 segments and matches 104,649 PRNG calls with 2,541 screens and cursors; its two new segments are independently chosen fresh recordings, seed 7730029 where a kitten takes the upstairs the hero vacated and seed 7710223 where an untamed monster crosses the downstairs, the second running `m_move()` rather than `dog_move()`. Removing the admission fails both, along with the new unit test that also pins `LADDER` as still refused. All 1,705 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `88e21932488cb928be4c4d6b49a90cd244d446fe` | 2026-07-28 | review window closure, score change | `254 shown + 194 hidden = 448 total` | Development matches 98,436 PRNG values, 254 screens, and 254 cursors. The four screens above the `5e4db31` row come from admitting `STAIRS` and non-blocking doorways as hero destinations. This head closes the fourth full correctness pass over the repeated-simple-command slice, `e30ea05..4fc57d8`: 23 raw, 22 deduplicated, and 19 confirmed findings, all applied, with three rejected and none unverified or deferred. Nine were production findings, and one produced wrong output rather than a stop: admitting `D_ISOPEN` doorways made `test_move()`'s diagonal-exit refusal reachable while nothing read the hero's own square. A fresh recording of seed 990002 with inputs `ljjjy` measured it, and the same recording is now the slice's one known non-matching case, since the port stops fail-closed one screen short of C's zero-time refusal frame. The score is identical to its parent call for call and screen for screen, because every corrected path lies outside what the development sessions reach; that is why the two fresh matrices are the real gate, matching 97,771 calls with 2,367 screens and cursors, and 46,255 calls with 54 screens and cursors. All 1,704 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head. Audit metrics and every rejection are recorded in `QUALITY.json`. The fix commit is itself correctness debt for the next pass. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `5e4db311f275f3a28be791a4355e48de35f70993` | 2026-07-28 | score change | `250 shown + 194 hidden = 444 total` | Development matches 98,385 PRNG values, 250 screens, and 250 cursors. The shown value falls 14 screens below the superseded `7363380` candidate, and every one of those screens was credit the port had not earned: `64223f1` stopped hero movement onto a boulder and let an unsupported command boundary end a segment instead of failing it, so the sessions now stop where the port's owned behavior stops. No commit between the two candidates raised or lowered matching for any other reason, confirmed by scoring `7363380`, `530dadf`, `2b96c56`, and `64223f1` in a detached worktree. This head closes the five confirmed findings of the second repeated-command re-audit; the score is identical to its parent call for call and screen for screen, because the fog-cloud and encumbrance paths it corrects are not reached by the development sessions. That is also why the 12-case fresh matrix is the real gate: it matches 83,269 PRNG calls and all 2,351 complete screens, attributes, and cursors. All 1,684 tests, four generated-data checks, `check:namespace-members`, and `npm run quality -- --check` pass at this exact head. The range `e30ea05..5e4db31` still owed its full correctness pass when this row was written; `88e2193` closed that pass and supersedes the candidate label. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `7363380130986e84ba023d9c7791e866ee90d036` | 2026-07-27 | score change | `264 shown + 194 hidden = 458 total` | Development remains at 98,751 PRNG values, 264 screens, and 284 cursors after closing the safe-pet refusal continuation found by the first full audit. The exact committed head passes all 1,637 tests, four generated-data checks, and the quality gate. The checked-in six-case repeated-simple-command matrix now matches 18,120 PRNG calls and all 279 complete screens, attributes, and cursors; its refusal case continues through timed fleeing and the next four waits. Source-derived branch markers pin natural generation, the doorless doorway, stable no-time safe-wait tail, both wall modes, both pet outcomes, and the one-object description. This row was the current candidate until `5e4db31` supplied a later validated handoff; it is kept because the shown value changed after it. The shown value is a conservative development lower bound; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `aa57304216fd4e15b976bd7483e89a1ff55f8a93` | 2026-07-27 | score change | `259 shown + 194 hidden = 453 total` | Source-owned wall refusal, ordinary starting-pet interaction, and single-object floor description raised development matching to 98,598 PRNG values, 259 screens, and 277 cursors. The exact committed head passes all 1,629 tests and the four generated-data checks. Six fresh reproductions cover a 140-wait natural-generation path, wall messages enabled and disabled, both safe-pet outcomes, and floor description; together they match 17,809 PRNG calls and all 165 screens, attributes, and cursors. A one-wait extension exposes the next independent in-boundary monster-doorway gap, so repeated-command behavioral closure remains open. The shown value is a conservative development lower bound rather than a published 44-session result; the hidden value is carried from the published ancestor and remains uncertain. No local holdout ran. |
| `5affc313ef52c662772099857cabd36596676767` | 2026-07-27 | monsters review window closure | `241 shown + 194 hidden = 435 total` | A full correctness audit of `f97bd58..d29414a` over the monsters area confirmed 17 of 22 findings with none rejected or unverified. Five were production defects in the newly ported `mondata.c` functions, four of them caused by eight `AD_*`/`AT_*` constants `js/monsters.js` never exported, which left their branches comparing a number against `undefined`. All 17 are fixed. The score is unchanged because the running game does not call these functions yet; that is also why the 33-session result could not have caught them, and why the tests are their only gate. Those tests now pin `mstrength()` against the difficulty stored in every `monsters.h` entry and guard that every monster constant `js/mondata.js` reads is a number. All 1,625 tests and the generated-catalog checks pass; development matches 98,270 PRNG values, 241 screens, and 256 cursors. Audit metrics are recorded in `QUALITY.json`. The hidden value is carried from the published ancestor and remains uncertain; no local holdout ran. |
| `f8911ff71ac9800094d4251fe7911cb8bcceb7cc` | 2026-07-26 | score change | `241 shown + 194 hidden = 435 total` | Supplying the recorder account to `set_playmode()` authorizes debug mode, which the port previously denied on every `playmode:debug` session. Development rose to 98,270 PRNG values, 241 screens, and 256 cursors; no session regressed. All 1,635 tests pass. The nine recorded debug sessions are the evidence: none carries the denial message `wd_message()` prints, and matching begins to hold past the two `!wizard` short-circuits in `dungeon.c`. A locally recorded fresh case cannot check this unless the recording account appears in `WIZARDS`, so no fresh differential is claimed. The hidden value is carried from the published ancestor and remains uncertain; no local holdout ran. |
| `33523218ed285430300f14e725bf43928b8b65e1` | 2026-07-25 | simple second-command milestone closure | `207 shown + 194 hidden = 401 total` | The live game reaches the prompt after two simple commands across the exact 17-case fresh matrix, matching 46,255 PRNG calls and all 68 screens, attributes, and cursors. Two excluded prefixes separately match fresh C through the last supported prompt. All 1,613 tests and four generated-data checks pass; development matches 77,658 PRNG values, 207 screens, and 238 cursors. Correctness metrics are recorded in `QUALITY.json`, and the full clarity report is retained at `/tmp/simple-second-command-clarity-report.md`. The hidden value is carried from the published ancestor and remains uncertain; no local holdout ran. |
| `a61681496d5712aa17ddee750d1a72a5275ab627` | 2026-07-24 | score change | `206 shown + 194 hidden = 400 total` | A live first-move object case raised the development result to 206 screens, with 77,588 PRNG values and 243 cursors. The hidden value is carried from the published ancestor and is uncertain. |
| `3b6c38de148679a5cc8313d755ec906fa95627c3` | 2026-07-23 | live slice and milestone closure | `205 shown + 194 hidden = 399 total` | The first complete gameplay turn closed with all 1,290 tests passing. The 18-segment fresh matrix matched 48,825 PRNG calls and all 55 complete screens, attributes, and cursors. Composite review and simplification coverage is recorded in `QUALITY.json`; the hidden value is carried and uncertain. |
| `270731e88cb25c3813820b7f4d516a9763719061` | 2026-07-23 | score change | `205 shown + 194 hidden = 399 total` | Source-shaped safe-wait handling raised the development result to 205 screens while retaining 77,588 PRNG values and 243 cursors. The hidden value is carried and uncertain. |
| `68472ba3aa99786e5c3e01f4407b07bc853ea89b` | 2026-07-23 | score correction | `204 shown + 194 hidden = 398 total` | Explicit replay and monster-action stops reduced the development result from 302 to 204 screens by removing credit beyond behavior the port owned. A four-way reproduction confirmed the attribution. The hidden value became especially uncertain after the correction. |
| `0708853dc18b2a0723dab61f6d116f7606fc23ab` | 2026-07-22 | score change | `302 shown + 194 hidden = 496 total` | Runtime monster creation raised the development lower bound to 302 screens, 78,645 PRNG values, and 590 cursors. This was never a published 44-session result; the hidden value was carried and uncertain. |
| `ddb23823ac69f27906257ee615a4bdb818deeaac` | 2026-07-22 | score change | `301 shown + 194 hidden = 495 total` | Source ambient sounds reduced the development result by one already-divergent level-two screen while fresh source-matching cases passed. The hidden value was carried and uncertain. |
| `76109518c4e879b444ee53cddd9617f875780888` | 2026-07-22 | score change | `302 shown + 194 hidden = 496 total` | Live command decoding and turn intent raised the development lower bound to 302 screens, 78,350 PRNG values, and 590 cursors. This was not a published result, and the hidden value was carried and uncertain. |
| `f0624a759f50fbf061ab7e48ff7e83a08ea57ef1` | 2026-07-22 | first-command milestone closure | `249 shown + 194 hidden = 443 total` | The 107-case closure matrix matched 311,439 PRNG calls, 119 screens, and every cursor; all 997 tests passed. Final review coverage is recorded in `QUALITY.json`, with the non-ledger clarity report retained in the pre-compaction Git history. The score is carried from the published ancestor. |
| `8866764466effe643de8f2020fc3e6612ed21bf4` | 2026-07-22 | published result | `249 shown + 194 hidden = 443 total` | The published leaderboard reported 249 public and 194 held-out points for its 2026-07-22 18:31 UTC run. A clean detached development run matched 213 screens. The local holdout was not run. |
| `e0f8b050a5215a2a999f10c75b8c574d6682e2a4` | 2026-07-22 | score correction | `213 shown + 0 hidden = 213 total` | A clean detached rerun matched 213 screens, 78,280 PRNG values, and 418 cursors; the previously recorded 217-screen result did not reproduce. No published hidden result existed yet. |
| `2b88f27dab4d6fc0af5dd50673cd4de6d87bfdb2` | 2026-07-22 | score change | `217 shown + 0 hidden = 217 total` | Source vault retry behavior raised the development result by 26 to 217 screens. This result was later corrected when it did not reproduce. |
| `5d3bfb9a5232fd966df86b678b6a27651eea0de4` | 2026-07-22 | score change | `191 shown + 0 hidden = 191 total` | Live optional themed-room fills raised the development result by 13 to 191 screens; broad first-command validation remained open. |
| `f140abfaa499b2e8d7648fffc5c0fb73e33a5c73` | 2026-07-21 | score change | `178 shown + 0 hidden = 178 total` | Startup and display work raised the development result by six to 178 screens. Fresh Enhanced1 and IBM cases matched completely; arbitrary first-prompt parity remained open. |
| `0ad24831c0de0d098af381520c8b85449bbd0802` | 2026-07-21 | score change | `172 shown + 0 hidden = 172 total` | Visible engraving reveal behavior raised the development result by six to 172 screens, with a fresh exact differential through the first prompt. |
| `1be5455e9c24c4a6515c7a176a62f5d7aca3fd8b` | 2026-07-21 | score change | `166 shown + 0 hidden = 166 total` | Source-ordered explore-mode notice handling raised the development result by four to 166 screens. Earlier level-generation divergence still blocked whole-session parity on sampled seeds. |
| `9dd455841a755c4506ee821b45fc741b1bbef43c` | 2026-07-21 | score change | `162 shown + 0 hidden = 162 total` | Source-shaped post-level startup replaced replay scaffolding and raised the development result to 162 screens. Earlier level-generation divergences remained. |
| `e143b964e1e12f458cbccab0c14521cef42ca259` | 2026-07-20 | initial baseline | `103 shown + 0 hidden = 103 total` | The fixed development set matched 103 screens; 286 tests and both then-declared generated-catalog checks passed. |
