# Expected leaderboard score

This tracker is newest first. Each row describes the code at the exact commit
shown, not a later tracker-only commit. Hidden estimates remain conservative
unless an authorized aggregate evaluation or published leaderboard result
provides evidence for a higher value.

| Code commit | Date | Expected screen score | Evidence and uncertainty |
| --- | --- | --- | --- |
| `f140abfaa499b2e8d7648fffc5c0fb73e33a5c73` | 2026-07-21 | `178 shown + 0 hidden = 178 total` | `node scripts/score-development.mjs` matched 178 screens across the fixed 33-session development set, six more than the preceding implementation. All 729 tests and the monster, object, symbol, and themed-room generated-data checks passed. Fresh Enhanced1 and IBM configurations matched all 3,028 and 2,394 PRNG calls, their complete first-prompt screens, and cursors; Chromium also verified custom RGB glyph projection and full-screen restoration. Seven independent full correctness passes plus post-fix reconciliation produced 110 raw, 105 deduplicated, 86 confirmed, 51 applied, 35 deferred, 19 rejected, and 0 unverified findings; two parallel reviews of the final delta found no remaining defect. The remaining deferrals are broader unported branches or controlled test gaps, so arbitrary first-prompt parity is not yet claimed. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `609efc6a45627fef08e08e8dd82eac2544188b94` | 2026-07-21 | `172 shown + 0 hidden = 172 total` | `node scripts/score-development.mjs` still matched 172 screens across the fixed 33-session development set after the startup audit fixes. All 688 tests passed. A fresh two-reroll Monk differential matched all 3,135 PRNG calls, four screens, and four cursors; a fresh standalone `G_*` configuration on seed 424242 matched all 2,033 PRNG calls, its first-prompt screen, and cursor. The startup full audit reported 10 raw, deduplicated, and confirmed findings; the production mismatch, documentation risks, and practical test gaps were addressed, while the controlled occupied-stair integration case remains deferred behind existing source-shaped component coverage. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `48ec9a8adbab826305a93f4108bf270535308064` | 2026-07-21 | `172 shown + 0 hidden = 172 total` | `node scripts/score-development.mjs` still matched 172 screens across the fixed 33-session development set after the themed-room correctness audit follow-ups. All 685 tests and the generated themed-room check passed. A fresh seed 424242 Valkyrie/dwarf differential matched all 2,033 PRNG calls, its first-prompt screen, and cursor; a second arbitrary configuration advanced to the known next unported themed-fill boundary. The repeat full correctness audit produced 10 raw and deduplicated findings, confirmed nine contract/test gaps, rejected one, and found no production correctness defect; after the gaps and two independently found composition defects were fixed, two parallel read-only reconciliations found no remaining issue. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `0ad24831c0de0d098af381520c8b85449bbd0802` | 2026-07-21 | `172 shown + 0 hidden = 172 total` | `node scripts/score-development.mjs` matched 172 screens across the fixed 33-session development set after visible engravings gained the source `newsym()` reveal and layer behavior, six more than the preceding implementation. All 677 tests passed. A fresh seed 2718281828 differential matched all 2,877 PRNG calls, its full first-prompt screen including the formerly missing engraving, and the cursor. A read-only source review caught and then verified the raw-symbol, inverse-video, and UTF-8 browser-attribute cases. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `7f0d4f1ba975b8134a45c70bae4cc80e6a329550` | 2026-07-21 | `166 shown + 0 hidden = 166 total` | `node scripts/score-development.mjs` matched 166 screens across the fixed 33-session development set after themed-room dispatch began failing before mutation, rejecting alternate state ownership, preserving injected fallback RNG, and using the source `rn1(3,2)` expansion. All 675 tests and the generated themed-room check passed. Four of six fresh first-prompt differentials matched every PRNG call, screen cell, attribute, and cursor; a fifth matched all 2,877 PRNG calls but exposed one missing engraving glyph, and the sixth advanced to the next unported themed-fill callback. A repeat full correctness audit reported no production correctness defect, two contract-clarity findings, and seven test gaps; those follow-ups are intentionally outside this exact code commit. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `12bea800ba8a34a4fbc08c32e1bb7fa6bff95793` | 2026-07-21 | `166 shown + 0 hidden = 166 total` | `node scripts/score-development.mjs` still matched 166 screens across the fixed 33-session development set after a formal simplification pass consolidated themed-room shuffling, environment construction, timer traversal, and test helpers without changing the source-shaped boundary. All 670 tests passed. The simplification pass found 10 candidates, confirmed and applied seven, and rejected three as behavior-risking or source-worse. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `5f5a8b718ecce8fae86f3b217f8588ce81c73ece` | 2026-07-21 | `166 shown + 0 hidden = 166 total` | `node scripts/score-development.mjs` still matched 166 screens across the fixed 33-session development set after source-derived themed-room definitions, selection primitives, initial fill handlers, map dispatch, and the partial live integration boundary were added. All 670 tests and the generated themed-room check passed. Six newly chosen first-prompt recordings consistently localized the next source-label mismatch to room relocation; several otherwise matched screens, providing broad seed and character evidence without tuning to a fixture. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `1be5455e9c24c4a6515c7a176a62f5d7aca3fd8b` | 2026-07-21 | `166 shown + 0 hidden = 166 total` | `node scripts/score-development.mjs` matched 166 screens across the fixed 33-session development set after the Unix explore-mode notice was placed between `newgame()` and the moveloop preamble, four more than the preceding implementation. All 631 tests passed. The end-to-end regression proves that pending welcome text reaches its `--More--` boundary before the notice and before the two preamble draws; fresh C recorder evidence confirms that order, although strict whole-session comparison remains blocked by an earlier known `mklev` divergence on the sampled seeds. A read-only source audit found no defect in the scoped delta. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `9dd455841a755c4506ee821b45fc741b1bbef43c` | 2026-07-21 | `162 shown + 0 hidden = 162 total` | `node scripts/score-development.mjs` matched 162 screens across the fixed 33-session development set after source-shaped hero placement, starting pets, inventory generation, rerolls, equipment, discoveries, spells, skills, status rendering, and initial vision replaced the post-`mklev` replay scaffold. All 628 tests and the generated object, monster, themed-room, and symbol checks passed. Fresh differentials covered every valid role/race pairing, rerolls, pets, long names, status options and conditions, and blind startup; a Chromium smoke test reached the first command prompt. An independent integration audit found no remaining post-`mklev` startup defect. Earlier `mklev` divergences still prevent broad arbitrary-seed parity, and the sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `13f1278b472fc8a65164c1a5b9fb56ce946ecc2d` | 2026-07-21 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` matched 103 screens across the fixed 33-session development set after ordinary-room population and mineralization replaced 1,448 trace-derived replay calls. All 544 tests and the generated object, monster, themed-room, symbol, and random-text checks passed. Fresh default, DEC, IBM, and UTF-8 differentials reached the next unported pet boundary, and a Chromium smoke test verified the browser-specific glyph representations. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `61f749ebfb720eaf18f422b18d5eb4ab1d02c8e4` | 2026-07-21 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` still matched 103 screens across the fixed 33-session development set after source-backed artifact conversion, random text and engravings, grave and terrain ownership, trap victims, candle timers, and mobile light projection were ported as ordinary-room prerequisites. All 504 tests and the generated object, monster, themed-room, and random-text checks passed. Independent source audits found no remaining defect in the artifact, trap, or candle-light paths. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `0753d2ab26890a99d6cb4b39b23e6c69bee4a0d6` | 2026-07-21 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` still matched 103 screens across the fixed 33-session development set after buried ownership, `mkcorpstat`, monster-backed objects, and container population were ported as dormant level-population prerequisites. All 452 tests and both generated object/monster checks passed; an independent source audit found no semantic or PRNG-order defect, including the added rare-branch coverage. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `59ecb69f516447700d30f2554b718f16ab9d154e` | 2026-07-21 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` still matched 103 screens across the fixed 33-session development set after source-shaped room coordinate selection and dynamic occupancy were ported. Nine focused tests passed, including source retry-boundary and raised-drawbridge quirks, and an independent read-only comparison found no correctness or PRNG-order defects. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `7112b46e46c0db0ae73d9b7e0bae0f447a9a1cc2` | 2026-07-21 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` still matched 103 screens across the fixed 33-session development set after initial trap ownership and selection were ported; all 424 tests passed. A newly chosen seed 424242 differential matched through the structural level build and first diverged at the next unported ordinary-room fill countdown in `makelevel`. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `86e0d7d37254193729572d4a723512af7a17dc1b` | 2026-07-21 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` matched 103 screens across the fixed 33-session development set after the artifact identifiers were switched to their canonical generated definitions; all 418 tests and `npm run check:monsters` passed. The simplification does not change identifier values or control flow. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `88ccb28d900bfc81e76f092f7b34218dbcbabba9` | 2026-07-21 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` still matched 103 screens across the fixed 33-session development set after the reviewed vault-realization fixes and verified simplification; all 392 tests passed. An arbitrary fresh differential still first diverges at PRNG call 1468 in the next unported ordinary-room population work, with matching cursor output. The sealed holdout was not run, so 0 hidden remains an unmeasured conservative estimate. |
| `71e40ad128009c7959c361aaa7946f10a18528fe` | 2026-07-20 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` matched 103 screens across the fixed 33-session development set; all 374 tests and the themed-room generated-data check passed, and both quality gates are clear. An arbitrary fresh differential matches C through PRNG call 1436 and first diverges at the known next unported `fill_special_room` boundary at call 1437. The sealed holdout was not run, so 0 hidden is an unmeasured conservative estimate. |
| `db2ab36938751e3d49b9385a014a3074784669bb` | 2026-07-20 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` matched 103 screens across the fixed 33-session development set; all 370 tests and the object, monster, and dungeon generated-data checks passed. Full review and simplification gates are clear. An arbitrary fresh differential reaches a known unported special-level generation boundary, so the sealed holdout remains unrun and 0 hidden is an unmeasured conservative estimate. |
| `ccb4f56274140b9a54829662cc9d6bca565bf0c7` | 2026-07-20 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` matched 103 screens across the fixed 33-session development set; all 346 tests committed at this SHA, both generated-catalog checks, and three strict fresh pre-level differentials passed. The sealed holdout was not run, so 0 hidden is an unmeasured conservative estimate. |
| `e143b964e1e12f458cbccab0c14521cef42ca259` | 2026-07-20 | `103 shown + 0 hidden = 103 total` | `node scripts/score-development.mjs` matched 103 screens across the fixed 33-session development set; 286 tests and both generated-catalog checks passed. The sealed holdout was not run, so 0 hidden is an unmeasured conservative estimate. |
