{
  "mode": "implementation",
  "reason": "The slice has not started. Every candidate that carries the behavior is `missing`: display.c map_invisible() has no port, glyph_is_invisible() reads a boolean sidecar nothing writes instead of C's glyph number, map_glyphinfo() has no GLYPH_INVIS_OFF arm, do_name.c x_monnam()'s do_it arm refuses, and mon.c mondead()'s clear stops. `missingWorkByOwner` orders the four groups so each prerequisite precedes the groups that use it.",
  "commitChecked": "b4a8f01aed5dc8d2f6965592c4dd2f5ab97524ae",
  "boundary": {
    "goalId": "remembered-invisible-monster",
    "startingCommit": "b4a8f01aed5dc8d2f6965592c4dd2f5ab97524ae",
    "startingEvent": "A player command that gives a monster its move reaches mhitm.c mattackm() through allmain.c moveloop_core(), monmove.c dochug() and, for a pet, dogmove.c dog_move(), and mattackm() calls hitmm() or missmm() with gv.vis TRUE and at least one participant the hero cannot spot. In seed0006-wizard-water-demon.session.json that is the byte H at step 77, the kitten biting a kobold in an unlit corridor.",
    "endingEvent": "The screen the port emits at the end of that step: its message line and its map. For seed0006 step 77 the message line is `You hear some noises.  The kitten bites it.` and column 62, row 9 still reads `#`, because the kobold dies from that bite and mon.c mondead():3170-3171 clears the marker before m_detach() repaints the square.",
    "validInputs": "Any command that advances the turn while a monster attacks another monster with gv.vis TRUE. Both participants ordinary monsters, neither an M_AP_TYPE mimic, mdef->mundetected already 0 because mattackm():328-350 clears a hidden defender above pre_mm_attack(). Covered by seed0006 step 77 for the write-and-clear-within-one-step case, and by a fresh recording for the case where the defender survives and the marker persists across steps.",
    "observables": "The remembered glyph number at the marked square, which must be GLYPH_INVISIBLE rather than a boolean sidecar; the drawn character, colour and attribute the marker resolves to through map_glyphinfo(), which C spells at display.c reset_glyphmap():3029-3035 as SYM_INVISIBLE + SYM_OFF_X, invis_color() = NO_COLOR and MG_INVIS; the message line from hitmm()/missmm(), whose Monnam()/mon_nam_too() must answer `it` for a defender the hero cannot spot; the random-number call sequence, which this slice must leave unchanged since neither map_invisible() nor x_monnam()'s do_it arm draws; and the cursor.",
    "exclusions": "The hero-facing marker sites in mhitu.c at 91, 553, 668, 685 and 1156, which js/mhitu.js:272, :539 and :845 already assign to later goals. display.c unmap_invisible()'s TRUE arm and its detect.c dosearch0() consumer, plus the hack.c domove_core() and pickup arms at js/hack.js:1966, :2035 and :2238, which need a marker that outlives its step and so need a recording of their own. x_monnam()'s augment_it, do_hallu, do_invis and priest/minion/shopkeeper arms. mon.c seemimic(), behind pre_mm_attack()'s two M_AP_TYPE arms. mon.c monstone():3358, apply.c, dokick.c, uhitm.c, zap.c, detect.c, sounds.c, muse.c and the other 60-odd map_invisible() callers, whose commands are unported."
  },
  "candidateListMethod": {
    "upstreamEntryPoints": "mhitm.c pre_mm_attack() (41-72) and its two callers missmm() (75-93) and hitmm() (645-...); display.c map_invisible() (377-385) and unmap_invisible() (387-396); do_name.c x_monnam() (826-1032) with its wrappers mon_nam() (1041), Monnam() (1073), noit_mon_nam() (1053), some_mon_nam() (1064) and mon_nam_too() (1190); mon.c mondead() (3170-3171).",
    "dispatchTablesAndCatalogs": "include/display.h's glyph enum, where GLYPH_INVIS_OFF (504) sits one place below GLYPH_DETECT_OFF and GLYPH_INVISIBLE (549) aliases it, so the marker occupies exactly one number rather than a NUMMONS-wide range. display.c reset_glyphmap()'s descending if-else chain (2739-3086), whose GLYPH_INVIS_OFF arm is 3029-3035. The symbol catalog entry SYM_INVISIBLE, already generated into js/symbol_data.js:154 as s_invisible, and MG_INVIS, which the port does not yet define beside its other MG_* constants at js/display.js:1448-1463.",
    "reachableHelpers": "Every C site that writes or reads the marker was found with `grep -rn 'glyph_is_invisible|GLYPH_INVISIBLE|map_invisible|unmap_invisible' src/ include/ win/tty/` over nethack-c/upstream. That listed 63 map_invisible() calls, 2 unmap_invisible() definitions with 12 calls, and 34 glyph_is_invisible() reads. The reads inside display.c are show_mon_or_warn() (489), mon_overrides_region() (699), feel_location() (764 and 830) and newsym() (1032); outside it, mon.c mondead() (3170) and monstone() (3358), lock.c pick_lock() (579-584 through an oldglyph comparison), plus readers in hack.c, apply.c, uhitm.c, dokick.c, dothrow.c, zap.c, detect.c, sounds.c, pager.c, cmd.c, glyphs.c, potion.c and wizcmds.c whose commands are unported.",
    "javascriptCrossCheck": "`grep -rn 'glyph_is_invisible|map_invisible|GLYPH_INVIS|invisible_monster' js/` found the port's six readers -- js/display.js:2129 (same_remembered_glyph), :2172 (feel_location), :2397 (unmap_invisible), js/mon.js:1760 (mondead), js/zap.js:644 and js/detect.js:577 -- the two unmap_invisible() callers at js/dokick.js:352 and js/detect.js:798, the constant at js/glyph_offsets.js:43, and the named refusals at js/mhitm.js:192 and :195, js/mhitu.js:272, :539 and :845, js/hack.js:1229 and :1966, js/uhitm.js:282, :307, :533 and :1559, and js/detect.js:527 and :578. `grep -n 'export function newsym' js/display.js` located the port's restructured newsym() at :2585, which has neither C's marker arm nor mon_overrides_region()'s marker term.",
    "remainingLimits": "The port's newsym() is a restructuring of C's, not a line-for-line port, so a marker arm has to be placed by reading the port's control flow rather than by matching a line number; a misplacement would show only as a wrong repaint. seed0006 writes and clears the marker inside one step, so it cannot witness the drawn character at all: the GLYPH_INVIS_OFF resolution and newsym()'s marker arm need a fresh recording where the defender survives the round. monsterCommonName() at js/do_name.js:227 is the port's partial mon_nam() and has call sites throughout js/; whether admitting do_it changes any of them other than the intended one is a question the list answers per site, not in bulk."
  },
  "entries": [
    {
      "candidate": "mhitm.c pre_mm_attack() (67-68), `if (!canspotmon(mdef)) map_invisible(mdef->mx, mdef->my);`",
      "reachability": "gv.vis TRUE, set at mattackm():355-357 from `(cansee(magr) && canspotmon(magr)) || (cansee(mdef) && canspotmon(mdef))`, with the defender not spottable. Runs before hitmm()/missmm() print anything and draws no random number.",
      "owner": "js/mhitm.js pre_mm_attack()",
      "effects": "Writes GLYPH_INVISIBLE into the defender's square in map memory and draws it, unless the square is the hero's own.",
      "evidence": "The port refuses at js/mhitm.js:195, `unsupported('an unseen defender marked on the map')`. seed0006 step 77 reaches it.",
      "status": "missing",
      "remaining": "Port display.c map_invisible() and call it here."
    },
    {
      "candidate": "mhitm.c pre_mm_attack() (63-64), `if (!canspotmon(magr)) map_invisible(magr->mx, magr->my);`",
      "reachability": "Same gv.vis guard, with the aggressor not spottable. gv.vis can still be TRUE through the defender half of the disjunct, so the arm is reachable.",
      "owner": "js/mhitm.js pre_mm_attack()",
      "effects": "Same write at the aggressor's square.",
      "evidence": "The port refuses at js/mhitm.js:192, `unsupported('an unseen attacker marked on the map')`. It is the same map_invisible() call as the defender arm, so it closes with it.",
      "status": "missing",
      "remaining": "Call the ported map_invisible() here too. No development session is known to reach this arm, so it closes on a unit test unless the fresh recording produces one."
    },
    {
      "candidate": "mhitm.c pre_mm_attack() (48-51 and 55-58), the two M_AP_TYPE arms calling mon.c seemimic()",
      "reachability": "Either participant disguised as an object or furniture.",
      "owner": "js/mhitm.js pre_mm_attack(), refusing",
      "effects": "Would reveal the mimic and set showit.",
      "evidence": "js/mhitm.js:178 and :184 refuse. mon.c seemimic() is unported and belongs to no part of this goal's boundary.",
      "status": "later",
      "remaining": "Stays refused. The refusal must survive this slice unchanged."
    },
    {
      "candidate": "mhitm.c pre_mm_attack() (52-54 and 59-61), the two mundetected clears",
      "reachability": "A hidden participant that is not a mimic.",
      "owner": "js/mhitm.js pre_mm_attack()",
      "effects": "Clears mundetected and sets showit.",
      "evidence": "Both are ported at js/mhitm.js:179-182 and :185-188. The defender's cannot fire, because mattackm():328-350 clears a hidden defender above this call; the port records that at js/mhitm.js:167-170.",
      "status": "done",
      "remaining": "None."
    },
    {
      "candidate": "mhitm.c pre_mm_attack() (65-66 and 69-70), the two `else if (showit) newsym(...)` arms",
      "reachability": "A spottable participant whose mundetected the arms above just cleared.",
      "owner": "js/mhitm.js pre_mm_attack(), through the injected `redraw` operation",
      "effects": "Redraws the participant's square.",
      "evidence": "Ported at js/mhitm.js:193 and :196. Unchanged by this slice, but the C `if/else if` chain means a marker write and a redraw are mutually exclusive per participant, which the port must keep.",
      "status": "done",
      "remaining": "None."
    },
    {
      "candidate": "display.c map_invisible() (378-385), the whole function: the hero-square guard, the svl.level.flags.hero_memory guard on the memory write, and the unconditional show_glyph()",
      "reachability": "Every caller. Inside this boundary, pre_mm_attack()'s two arms.",
      "owner": "None",
      "effects": "levl[x][y].glyph = GLYPH_INVISIBLE and a glyph-buffer write. Draws no random number.",
      "evidence": "No js/ definition; js/display.js:2367-2377 records its absence as the reason glyph_is_invisible() reads a sidecar.",
      "status": "missing",
      "remaining": "Port it into js/display.js. The hero-square guard and the hero_memory guard are separate conditions and each needs its own test; hero_memory is TRUE in every ported game, so a test has to set it FALSE to pin the second."
    },
    {
      "candidate": "include/display.h glyph_is_invisible() (773), `((glyph) == GLYPH_INVISIBLE)`",
      "reachability": "Six ported readers plus map_glyphinfo().",
      "owner": "js/display.js glyph_is_invisible() (2378)",
      "effects": "None; a predicate.",
      "evidence": "The port takes a location record and reads `location?.remembered_glyph?.invisible_monster`, a boolean sidecar nothing writes. C compares a glyph number. The goal's testable property is that the port compares the number too.",
      "status": "missing",
      "remaining": "Change the signature to take a glyph number and move all six readers with it: js/display.js:2129, :2172, :2397, js/mon.js:1760, js/zap.js:644, js/detect.js:577."
    },
    {
      "candidate": "display.c reset_glyphmap() (3029-3035), the GLYPH_INVIS_OFF arm: `sym.symidx = SYM_INVISIBLE + SYM_OFF_X`, `invis_color(offset)` = NO_COLOR, `glyphflags |= MG_INVIS`",
      "reachability": "Any draw of a square holding the marker.",
      "owner": "js/display.js map_glyphinfo() (1570), which has no arm for the number, and mapGlyphinfoResolves() (1535), which rejects it",
      "effects": "Resolves the marker to the drawn character, colour and attribute.",
      "evidence": "mapGlyphinfoResolves() admits only GLYPH_NOTHING_OFF, objects and non-zap cmaps. SYM_INVISIBLE exists at js/const.js:2551 and s_invisible at js/symbol_data.js:154, so no generated table changes; MG_INVIS is not defined among js/display.js:1448-1463.",
      "status": "missing",
      "remaining": "Add the arm, widen the guard, and define MG_INVIS. seed0006 cannot witness the drawn character, so this needs the fresh recording where the marker survives a step, or a unit test pinned to the C source."
    },
    {
      "candidate": "display.c newsym() (1032-1033), `else if (glyph_is_invisible(lev->glyph)) map_invisible(x, y);`",
      "reachability": "A visible square with no monster to display and no warning, whose memory already holds the marker. Reached on every repaint of such a square, including docrt().",
      "owner": "js/display.js newsym() (2585), which has no such arm",
      "effects": "Re-asserts the marker rather than recomputing the square from its layers.",
      "evidence": "The port's newsym() is a restructuring; its layer choice would overwrite the marker with the underlying terrain, silently. seed0006 cannot show this, because its marker is cleared in the same step it is written.",
      "status": "missing",
      "remaining": "Place the arm in the port's control flow at the point C reaches it, and cover it with the fresh recording where the marker survives a step."
    },
    {
      "candidate": "display.c show_mon_or_warn() (486-493), the `if (glyph_is_invisible(levl[x][y].glyph))` clear that calls unmap_object() and re-maps a visible object",
      "reachability": "Drawing anything on the monster layer at a square that holds the marker.",
      "owner": "js/display.js newsym()'s inlined display_monster arms",
      "effects": "Stops remembering the marker and starts remembering a visible object under it.",
      "evidence": "C's comment at 484-486 states the invariant: the marker lives in the object layer, so putting a monster there must clear it. The port has no such clear, because nothing wrote a marker to clear.",
      "status": "missing",
      "remaining": "Port the clear. Its second half, re-mapping a visible object, needs vobj_at() and map_object(), both of which the port has."
    },
    {
      "candidate": "display.c mon_overrides_region() (697-699), `return glyph_is_invisible(levl[mx][my].glyph) ? TRUE : FALSE;`",
      "reachability": "newsym() on a square inside a visible gas region that also holds the marker.",
      "owner": "js/display.js newsym()'s region block, which computes monsterSensed, monsterWarning and adjacentVisibleMonster but has no marker term",
      "effects": "Decides whether the region or the marker is drawn.",
      "evidence": "The port's region block has three terms where C's helper has four.",
      "status": "missing",
      "remaining": "Add the term. No ported path generates a gas region yet, so this closes on source reading and a unit test rather than a recording; confirm that before writing it, and if no region can exist, restate as cannot-occur with the C evidence."
    },
    {
      "candidate": "display.c feel_location() (763-767), the accurate-memory return `if (glyph_is_invisible(lev->glyph) && m_at(x, y)) return;`",
      "reachability": "A blind or feeling hero at a square holding both the marker and a monster.",
      "owner": "js/display.js feel_location() (2172)",
      "effects": "Leaves the marker alone so searching does not rediscover the monster every turn.",
      "evidence": "Ported and correct in shape; it changes only with glyph_is_invisible()'s signature. js/display.js:2170-2171 records that it never returns yet.",
      "status": "missing",
      "remaining": "Move it to the glyph-number predicate. Its first TRUE answer arrives with map_invisible(), so a test must construct the state rather than rely on a recording."
    },
    {
      "candidate": "display.c feel_location() (829-836), the boulder-or-marker arm that suppresses the room glyph",
      "reachability": "A feeling hero at a square whose memory holds a boulder or the marker.",
      "owner": "js/display.js feel_location()",
      "effects": "Chooses map_background() or the room glyph rather than keeping the remembered glyph.",
      "evidence": "The port carries the boulder half; the marker half rides on the same predicate.",
      "status": "undecided",
      "remaining": "Read the port's feel_location() layer choice against display.c:822-838 and decide whether the marker disjunct is present, absent, or unreachable here. This entry must resolve before readiness."
    },
    {
      "candidate": "lock.c pick_lock() (579, 584), the oldglyph comparison that same_remembered_glyph() stands in for",
      "reachability": "A hero picking a lock on a door, where feel_location() may or may not change the square.",
      "owner": "js/display.js same_remembered_glyph() (2125)",
      "effects": "Decides whether pick_lock() reports that the door changed.",
      "evidence": "The port compares `glyph` and the `invisible_monster` sidecar because the marker had no number. With the number, C's plain `!=` on the glyph is the whole comparison and the sidecar term goes.",
      "status": "missing",
      "remaining": "Delete the sidecar term. A test must show the comparison still answers correctly for a square feel_location() left as it found it, which is why the wrapper exists."
    },
    {
      "candidate": "do_name.c x_monnam() (863-885), the do_it arm and the do_it predicate `!canspotmon(mtmp) && article != ARTICLE_YOUR && !program_state.gameover && mtmp != u.usteed && !engulfing_u(mtmp) && !(suppress & SUPPRESS_IT)`",
      "reachability": "Any name taken for a monster the hero cannot spot. Inside this boundary, hitmm()'s Monnam(magr) and mon_nam_too(mdef, magr), and missmm()'s Monnam(magr) and mon_nam_too(mdef, magr).",
      "owner": "js/do_name.js x_monnam() (279), which throws unless SUPPRESS_IT is set, and monsterCommonName() (227), the port's partial mon_nam(), which always returns `the <species>`",
      "effects": "Returns the string `it` and returns early, before the article, invisibility, saddle and naming work below.",
      "evidence": "js/do_name.js:270-278 records that the port admits only the SUPPRESS_IT | SUPPRESS_INVISIBLE | SUPPRESS_HALLUCINATION combination precisely to keep do_it, do_invis and do_hallu statically dead. The two partial spellings of one C function must be reconciled in this slice, because monsterCommonName() does not refuse: it would print `the kobold` where C prints `it`, which is a silent divergence rather than a fail-closed stop.",
      "status": "missing",
      "remaining": "Admit the do_it arm and route the port's mon_nam()/Monnam() through it. Each of the five predicate terms needs its own coverage; u.usteed and engulfing_u() may be unreachable, in which case say so with the C evidence rather than leaving them untested."
    },
    {
      "candidate": "do_name.c x_monnam() (877-882), the augment_it arm producing `someone`/`something`",
      "reachability": "Only through some_mon_nam()/Some_Monnam(), which pass AUGMENT_IT.",
      "owner": "None",
      "effects": "Substitutes a humanoid-aware word for `it`, and under hallucination draws rn2(2).",
      "evidence": "No caller inside the boundary: hitmm() and missmm() use Monnam() and mon_nam_too(), neither of which passes AUGMENT_IT. The arm also draws a random number, so admitting it without a caller would put an unspent draw in the port.",
      "status": "later",
      "remaining": "Stays out. The do_it arm must be written so that augment_it is FALSE on every path this slice admits."
    },
    {
      "candidate": "do_name.c noit_mon_nam() (1053-1060), which passes SUPPRESS_IT precisely to defeat do_it",
      "reachability": "Its own callers, outside this boundary.",
      "owner": "js/do_name.js alwaysVisibleMonsterName() (377)",
      "effects": "Names a monster as if the hero could always see it.",
      "evidence": "This is the one wrapper that must not inherit the new do_it arm. C makes that explicit by passing SUPPRESS_IT.",
      "status": "undecided",
      "remaining": "Confirm that the port's wrapper passes SUPPRESS_IT once x_monnam() admits do_it, and pin it with a test. Resolve before readiness."
    },
    {
      "candidate": "do_name.c mon_nam_too() (1190-1215), which returns mon_nam(mon) unless the two monsters are the same, in which case it uses a reflexive pronoun",
      "reachability": "hitmm() and missmm() both call it for the defender relative to the aggressor.",
      "owner": "js/mhitm.js mon_nam_too()",
      "effects": "The defender's name in the message.",
      "evidence": "Ported and already called at js/mhitm.js:218. It inherits do_it through mon_nam(), which is how `it` reaches the seed0006 message line.",
      "status": "missing",
      "remaining": "Nothing in mon_nam_too() itself changes; it becomes correct when mon_nam() does. Its reflexive arm is separate and stays as it is."
    },
    {
      "candidate": "mon.c mondead() (3170-3171), `if (glyph_is_invisible(levl[mtmp->mx][mtmp->my].glyph)) unmap_object(mtmp->mx, mtmp->my);`",
      "reachability": "Any monster death at a square holding the marker. In seed0006 step 77 the kobold dies from the bite that wrote the marker.",
      "owner": "js/mon.js mondead() (1760), which refuses with `forgetting a remembered invisible monster`",
      "effects": "Clears the marker from map memory before m_detach()'s newsym() repaints the square.",
      "evidence": "Without it the port stops one call after the message. With the marker written but this skipped, a stray `I` would sit at column 62, row 9 for the whole remaining stretch.",
      "status": "missing",
      "remaining": "Port the clear. seed0006 step 77 is the differential that covers it."
    },
    {
      "candidate": "display.c unmap_invisible() (387-396), the TRUE arm `unmap_object(x, y); newsym(x, y); return TRUE;`",
      "reachability": "detect.c dosearch0() and eleven other callers, over a marker that outlived the step that wrote it.",
      "owner": "js/display.js unmap_invisible() (2394), whose FALSE arm is ported and whose TRUE arm throws",
      "effects": "Clears the marker and repaints.",
      "evidence": "Both callees exist in the file. The arm is currently unreachable because nothing writes a marker; once map_invisible() lands it becomes reachable through js/dokick.js:352 and js/detect.js:798.",
      "status": "undecided",
      "remaining": "This is the entry that decides whether the goal needs a second slice. Determine whether either ported caller can reach a live marker within a game this slice's recordings produce. If it can, the arm lands here; if it cannot, restate as `later` with the C evidence and queue the second slice."
    },
    {
      "candidate": "js/zap.js:644 and js/detect.js:577, the port's two remaining glyph_is_invisible() readers (zap.c:4083, detect.c:1712)",
      "reachability": "Their own commands, none of which this boundary dispatches.",
      "owner": "js/zap.js and js/detect.js",
      "effects": "None inside the boundary.",
      "evidence": "Both read the predicate whose signature changes, so both are edited by this slice even though neither runs in it.",
      "status": "later",
      "remaining": "Move them to the glyph-number predicate. Their behavior is unchanged, and the full test suite plus an identical development score is the proof."
    },
    {
      "candidate": "The hack.c marker readers at 206, 1935, 2244, 2273 and 2889, and the write at 2902",
      "reachability": "domove_core(), test_move() and the pickup arms.",
      "owner": "js/hack.js:1229-1233, :1966, :2035 and :2238, all recorded as unreachable while map_invisible() is unported",
      "effects": "None yet.",
      "evidence": "Each site's comment states that the marker has no writer. Once map_invisible() lands, those statements stop being true and each becomes reachable in principle.",
      "status": "undecided",
      "remaining": "Re-read all six sites once map_invisible() lands and decide, per site, whether it can now fire inside a game this slice's recordings produce. Any that can and is not ported becomes a deferral with `npm run quality -- defer`, not a silent skip. Resolve before readiness."
    },
    {
      "candidate": "The mhitu.c marker sites at 91, 553, 668, 685 and 1156",
      "reachability": "A hero attacked by a monster it cannot spot.",
      "owner": "js/mhitu.js:272, :539 and :845, refusing",
      "effects": "None inside the boundary.",
      "evidence": "The goal entry assigns them to later goals; js/mhitu.js:272-275 records that the marker and the `it` name refuse together, which this slice half-resolves by porting the name.",
      "status": "later",
      "remaining": "Their refusals must survive this slice. Check that porting the name does not turn any of them from a fail-closed stop into a silent divergence; if one does, it becomes a deferral."
    },
    {
      "candidate": "mon.c monstone() (3358-3359), the same clear as mondead()'s",
      "reachability": "A monster turned to stone.",
      "owner": "None",
      "effects": "None inside the boundary.",
      "evidence": "No ported path petrifies a monster.",
      "status": "later",
      "remaining": "None for this slice."
    }
  ],
  "missingWorkByOwner": [
    {
      "group": "The marker's representation, in js/display.js",
      "candidates": [
        "include/display.h glyph_is_invisible() over the glyph number",
        "display.c reset_glyphmap()'s GLYPH_INVIS_OFF arm and MG_INVIS",
        "display.c map_invisible()",
        "lock.c pick_lock()'s oldglyph comparison through same_remembered_glyph()",
        "display.c feel_location()'s two marker sites",
        "js/zap.js and js/detect.js readers"
      ],
      "next": "Make glyph_is_invisible() compare C's number, add map_glyphinfo()'s GLYPH_INVIS_OFF arm with MG_INVIS so the number can round-trip through map memory, then port map_invisible(). Every other reader moves with the predicate in the same change, and the proof that nothing else moved is the full suite plus a development score identical call for call and screen for screen."
    },
    {
      "group": "The marker's lifetime on the map, in js/display.js and js/mon.js",
      "candidates": [
        "display.c newsym()'s marker arm",
        "display.c show_mon_or_warn()'s clear",
        "display.c mon_overrides_region()'s marker term",
        "mon.c mondead()'s clear",
        "display.c unmap_invisible()'s TRUE arm"
      ],
      "next": "Once a marker can exist, make it survive a repaint, yield to a monster drawn on top of it, and vanish when its monster dies. mondead()'s clear is what seed0006 step 77 needs; newsym()'s arm needs the fresh recording where the defender survives."
    },
    {
      "group": "The name `it`, in js/do_name.js",
      "candidates": [
        "do_name.c x_monnam()'s do_it arm",
        "do_name.c mon_nam()/Monnam() through monsterCommonName()",
        "do_name.c noit_mon_nam()'s SUPPRESS_IT"
      ],
      "next": "Admit do_it in x_monnam() and reconcile the port's two partial spellings of mon_nam(), so that a monster the hero cannot spot is named `it` and noit_mon_nam() still is not. This is a prerequisite of the marker, not a follow-up: hitmm() prints its message in the same breath, and the port's stand-in does not refuse."
    },
    {
      "group": "The consumer, in js/mhitm.js",
      "candidates": [
        "mhitm.c pre_mm_attack()'s two !canspotmon arms"
      ],
      "next": "Replace the two refusals with the map_invisible() calls, which is what makes seed0006 step 77 emit `You hear some noises.  The kitten bites it.` and leaves column 62, row 9 reading `#`."
    },
    {
      "group": "The sites this slice makes reachable without porting",
      "candidates": [
        "The hack.c marker readers and write",
        "The mhitu.c marker sites"
      ],
      "next": "Re-read every site whose comment says the marker has no writer, once one exists. Each must be a fail-closed stop or a ported path; a site that silently takes the wrong branch is what this entry exists to catch."
    }
  ],
  "sourceReview": "Not yet attested. The candidate list above was built by reading mhitm.c pre_mm_attack() and its two callers, display.c map_invisible(), unmap_invisible(), show_mon_or_warn(), mon_overrides_region(), feel_location(), newsym() and reset_glyphmap(), do_name.c x_monnam() with its six wrappers, and mon.c mondead() and monstone(), and by grepping the whole of nethack-c/upstream/src and include for every marker site. What it does not yet cover: the random-number order through hitmm() and mdamagem() after pre_mm_attack() returns, which the differential must confirm is unchanged; the port's newsym() control flow, which has to be read line by line before the marker arm can be placed; and the four entries left `undecided`, which are feel_location()'s boulder-or-marker arm, noit_mon_nam()'s SUPPRESS_IT, unmap_invisible()'s TRUE arm, and the six hack.c sites."
}
