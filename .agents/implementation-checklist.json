{
  "mode": "ready-for-audit",
  "reason": "The supported ASCII REG_NOSUB boundary is source-reviewed and validated. Finite brace intervals which repeat a referenced capture now stop before matching and are tracked in posix-finite-repeat-backreference-arrival; the audit proved that glibc's compiled-node arrival state cannot be represented by the existing capture-value heuristic without false positives and negatives.",
  "commitChecked": "5488a3b700cedb56349e3ff021d927457f0b26dd",
  "boundary": {
    "goalId": "options-config-parser",
    "startingCommit": "7eede9da56a3cffe2c82294a07cc039706f69a27",
    "startingEvent": "A valid ASCII POSIX ERE installed by startup MSGTYPE or AUTOPICKUP_EXCEPTION is matched through the recorder-glibc REG_EXTENDED|REG_NOSUB contract.",
    "endingEvent": "Matching selects the same boolean and MSGTYPE action as recorder glibc within the committed direct and reference time/memory budgets at exact 255-byte pattern and message limits.",
    "validInputs": "Direct ASCII EREs, supported reference-aware EREs, unbounded referenced repetitions, suffix-reaching correlated and zero-minimum reference patterns, and unanchored direct/reference wide-frontier patterns within the source byte limits.",
    "observables": "Match boolean and installed MSGTYPE action, complete fresh screens/cursors and PRNG calls, plus isolated wall time and maxRSS for every declared exact-boundary resource case.",
    "exclusions": "Finite brace intervals whose subtree captures a group used by a backreference stop at UnsupportedPosixFiniteReferenceError and are tracked by posix-finite-repeat-backreference-arrival. Non-ASCII C.UTF-8 locale matching, runtime autopickup object filtering, interactive MSGTYPE editing/serialization, USER_SOUNDS, and behavior after the next message/input boundary remain named before-effect boundaries."
  },
  "candidateListMethod": {
    "upstreamEntryPoints": "sys/share/posixregex.c regex_match delegates to glibc regexec; glibc capture endpoint selection defines the finite-repeat result; options.c msgtype_type and add_autopickup_exception are the installed consumers.",
    "dispatchTablesAndCatalogs": "The shared exact-boundary fixture catalog, its complete budget map, 255-byte source limits, repeat endpoint states, referenced capture ids, candidate starts, and absolute suffix constraints define the audited families.",
    "reachableHelpers": "The audit supplied direct-libc/live MSGTYPE discriminators for the endpoint defect and four suffix-reaching exact-boundary families that separately exercise correlated state, zero-minimum state, direct candidate ordering, and reference candidate reuse.",
    "javascriptCrossCheck": "Inspect js/posixregex.js repeat endpoint/continuation projections, direct/reference sequence search and suffix feasibility, plus all shared fixture/oracle/resource consumers for masked paths, separate catalogs, aborts, or guessed booleans.",
    "remainingLimits": "The catalog names literal-suffix guard coverage separately from suffix-reaching direct memo, reference memo, zero-minimum, and correlated-frontier cases. Exact finite-copy OPEN/CLOSE arrival provenance remains the explicit posix-finite-repeat-backreference-arrival deferral rather than a guessed boolean."
  },
  "entries": [
    {
      "candidate": "Finite-repeat capture and backreference arrival provenance",
      "reachability": "A bounded repeated group first captures a referenced descendant and may either stop at an intermediate count or continue through a nonparticipating sibling to its maximum.",
      "owner": "js/posixregex.js reference repeat evaluation",
      "effects": "glibc validates distinct compiled OPEN/CLOSE occurrences with check_arrival before a backreference can affect MSGTYPE or AUTOPICKUP_EXCEPTION.",
      "evidence": "Systematic recorder-libc comparison found both false positives and false negatives in capture-value endpoint heuristics. regex_compile still accepts the source-valid syntax, while regex_match now raises UnsupportedPosixFiniteReferenceError before a rule action is selected.",
      "status": "later",
      "remaining": "Port the lazy occurrence-aware state log and backreference arrival cache recorded by posix-finite-repeat-backreference-arrival, then restore its libc, live-message, and resource matrices."
    },
    {
      "candidate": "Reference late-constraint exact-boundary resource bound",
      "reachability": "A valid 255-byte suffix-reaching reference pattern materializes large repeat/capture frontiers before downstream references and fixed suffix structure decide viability.",
      "owner": "js/posixregex.js reference matcher",
      "effects": "Synchronous matching exceeds the committed 2,000 ms and 128 MiB budget while producing the correct boolean.",
      "evidence": "A correlated near-miss reached about 177 MiB, and a zero-minimum valid match reached about 4.4 seconds/943 MiB; recorder libc agreed on both booleans within the budget.",
      "status": "done",
      "remaining": "None; lazy continuation testing and incremental frontier deduplication preserve exact booleans. The zero-minimum valid match measured 152 ms/107 MiB and the correlated near-miss 859 ms/122 MiB under their 2,000 ms/128 MiB budgets."
    },
    {
      "candidate": "Direct suffix-aligned exact-boundary search",
      "reachability": "A nullable unanchored direct pattern has a rightmost suffix-aligned match after expensive earlier candidate starts.",
      "owner": "js/posixregex.js direct matcher",
      "effects": "Left-to-right candidate work exceeds the committed 1,000 ms budget despite REG_NOSUB exposing only a boolean.",
      "evidence": "The 255-byte wide-frontier pattern on a 255-byte suffix-reaching input matched like libc but independently measured about 1.2 seconds, over budget.",
      "status": "done",
      "remaining": "None; REG_NOSUB tries the suffix-aligned rightmost candidate first while every evaluator call retains its actual matchStart. The exact case measured 2.5 ms/49 MiB under its 1,000 ms/96 MiB budget."
    },
    {
      "candidate": "Suffix-reaching resource-path matrix",
      "reachability": "Every declared exact-boundary fixture should exercise the evaluator path named by its case rather than exit through the adjacent literal-suffix guard.",
      "owner": "scripts/startup-regex-fixtures.mjs, direct libc oracle, and resource runner/test",
      "effects": "Masked cases report millisecond green budgets while leaving direct memo, reference memo, zero-minimum, and correlated-frontier paths unmeasured.",
      "evidence": "All committed patterns end in b$ while their inputs end in a; both matchers return before allocating memo or entering candidate loops.",
      "status": "done",
      "remaining": "None; the catalog names one guard and four suffix-reaching 255-byte cases for direct memo, reference memo, zero-minimum, and correlated-frontier evaluation, each with a libc boolean and isolated budget."
    },
    {
      "candidate": "Single resource-case catalog owner",
      "reachability": "Adding an exact-boundary fixture should automatically reach the libc oracle, resource runner, and output-name assertion.",
      "owner": "scripts/startup-regex-fixtures.mjs and resource harness",
      "effects": "Separate formula, budget, and expected-name catalogs can silently omit measurement while tests stay green.",
      "evidence": "The oracle iterates the fixture array, but the resource runner iterates a separate budget map and its test copies expected names again.",
      "status": "done",
      "remaining": "None; the dependency-free fixture module owns formulas, booleans, budgets, resource enumeration, and derived output names. The fixed-point case remains separate and the test asserts complete ordered set equality."
    }
  ],
  "missingWorkByOwner": [],
  "sourceReview": "Complete for the stated boundary. Read sys/share/posixregex.c regex_match, glibc 2.39 regcomp.c parse_dup_op and regexec.c get_subexp/check_arrival, options.c msgtype_type/add_autopickup_exception, and the recorder libc oracle contract. The supported corpus has 135 recorder-libc regex comparisons, five vpline comparisons, six isolated resource cases, and 28 fresh MSGTYPE segments. Finite brace-repeat capture provenance is identified by a named before-effect stop and an open quality deferral."
}
